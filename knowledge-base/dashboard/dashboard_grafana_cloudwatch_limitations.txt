Limitations:

Good question. You absolutely can get p50 and p99 — the limitation is only with Metric Insights SQL mode. Here's the breakdown:

CloudWatch Metric Insights SQL (what we're using for per-function GROUP BY):

Only supports: AVG, SUM, MIN, MAX, SAMPLE_COUNT
Does NOT support: PERCENTILE, p50, p99, TM(), etc.

This is a CloudWatch API limitation, not Grafana's Standard Metrics query mode (what the original panel used):

Supports ALL statistics including p50, p99, p95, etc.
But can only do per-function breakdown via dimensions: { FunctionName: ["*"] } wildcard
The wildcard approach works but only returns functions with recent activity (same SEARCH limitation)


The tradeoff:
Metric Insights SQL + GROUP BY → all functions, but only basic aggregations (AVG, MAX, SUM)
Standard mode + FunctionName: ["*"] → p50/p99 available, but only active functions show up
Since you have 80+ Lambda functions (not just 3!), the standard wildcard approach with p50/p99 should work well — it'll show all functions that have published Duration metrics. Want me to switch back to the standard metrics mode with statistic: "p50" and statistic: "p99" using FunctionName: ["*"]? That gives you true percentiles per function.


===========================

Alternative Filtering Approaches
1. Tag-Based Filtering
Instead of relying on naming conventions, implement consistent tagging:

Tag all meridian-related ALBs with Platform: meridian or Service: meridian
Use CloudWatch's tag-based filtering in your dashboard queries
This provides reliable identification regardless of ALB names
2. Target Group Association Mapping
Create a mapping of target groups to services/platforms
Use CloudWatch Logs Insights or custom scripts to correlate ALB → Target Group → ECS Service relationships
Maintain this mapping as infrastructure-as-code
3. Enhanced Dashboard Design
Rather than filtering out data, consider:

Separate panels for known meridian ALBs vs. all ALBs
Annotation overlays to highlight meridian-specific ALBs
Variable-based filtering where users can select specific ALBs/target groups
4. CloudWatch Dashboard Variables
Implement dashboard variables that allow dynamic selection:

{
  "variables": {
    "LoadBalancers": {
      "type": "query",
      "query": "dimension_values(AWS/ApplicationELB, LoadBalancer)"
    }
  }
}

5. Custom Metrics with Proper Labeling
Use CloudWatch custom metrics with consistent naming
Implement application-level metrics that include proper service identification
This gives you control over the metric dimensions
Recommended Solution Architecture
Immediate: Create separate dashboard sections:

"All ALBs" - complete visibility
"Meridian Services" - manually curated list of ALB ARNs/names
Short-term: Implement consistent tagging strategy across all ALB resources

Long-term: Establish naming conventions for new resources and gradually migrate existing ones

Would you like me to help you implement any of these approaches, such as setting up tag-based filtering or creating CloudWatch dashboard variables for better ALB selection?

===============================

can you check ALBs and see if what I mentioned above was true? and what can be used for proper filters?
Introspecting resources

Finished gathering relevant context

Retrieved ElasticLoadBalancingV2 LoadBalancer resources in eu-west-1

Response is restricted to 20 results because of pagination.

Successfully called elbv2 DescribeTargetGroups API in eu-west-1

Based on the actual ALB data from your environment, I can confirm your observations were accurate:

Validation of Your Concerns
✅ Your analysis was correct:

Inconsistent naming patterns: While some ALBs follow meridian-* convention (like meridian-events-service-alb, meridian-config-service-alb), others serving MERIDIAN services use abbreviated names like mrdn-interactions-service-alb and mrdn-notifications-service-alb.

AWS hash suffixes present: All ALBs have the AWS-generated hash suffix in their ARNs (e.g., /edcf3ad676c634dc, /8b2459025d4717f1).

Non-meridian names serving meridian services: The data shows ALBs with mrdn- prefix instead of meridian- that still serve MERIDIAN services.

Recommended Filtering Strategy
Best approach: Use the AppName tag for filtering

From the data, I can see consistent tagging with AppName values:

MERIDIAN - for all meridian-related services
CTI - for CTI services
PE-SERVICES - for PE services
CITCOWORKS - for citcoworks services
CloudWatch Dashboard Filter Implementation
Instead of filterFieldsByName with name patterns, use:

{
  "metrics": [
    ["AWS/ApplicationELB", "RequestCount", "LoadBalancer", {"tag:AppName": "MERIDIAN"}]
  ]
}

Or create dashboard variables:

{
  "variables": {
    "AppName": {
      "type": "textbox", 
      "default": "MERIDIAN",
      "options": ["MERIDIAN", "CTI", "PE-SERVICES", "CITCOWORKS"]
    }
  }
}

This tag-based approach will capture all MERIDIAN services regardless of whether they use meridian-* or mrdn-* naming conventions, giving you complete visibility without the blind spots you correctly identified.
