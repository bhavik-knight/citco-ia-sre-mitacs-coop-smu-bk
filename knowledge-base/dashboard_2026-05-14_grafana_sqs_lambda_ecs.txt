2026-05-14
-----


Dashboard & Grafana

SQS Queue Depth panel — switched from $sqs_queue Grafana variable (showed 0 data for idle queues) to Metric Insights SQL GROUP BY QueueName to get all queues as separate time series, then removed LIMIT 100 and GROUP BY after discovering Grafana plots SQL GROUP BY as a single consolidated series not one per queue

SEARCH() vs Metric Insights investigation — confirmed SEARCH() only returns queues with recent CloudWatch activity (idle queues disappear), Metric Insights SQL doesn't support LIKE wildcard filtering, and GROUP BY returns one frame not multiple series — all three approaches have limitations for per-queue filtering

SQS Health gauge (Row 1) — fixed typo "type": "guage" → "gauge", updated thresholds to green 0–34999 / yellow 35000–49999 / red 50000+, removed text mappings (OK/WARNING/CRITICAL) so the actual number shows with colour coding, turned off showThresholdLabels

SQS Health composite critical logic — attempted to add three critical conditions (depth > 50K, any DLQ message, age > 1hr) using Grafana __expr__ math expression — hit multiple errors: ternary ? : not supported, unknown query type [B], invalid math command: expr: unexpected invalid character: ? — reverted to simple single-query gauge

DLQ Depth panel — changed from stat to timeseries panel type, switched to Metric Insights SQL WHERE QueueName LIKE '%dlq%' OR '%dead%' for per-DLQ filtering

Dashboard not updating in Grafana — discovered python main.py deploy alone doesn't push dashboard changes because the Lambda provisioner only re-runs when ProvisionerVersion changes in CFN — dashboard JSON lives in Secrets Manager and must be pushed separately

Automation

Auto build + publish hook — added --deploy flag to build_dashboard.py so one command does everything: build rows → push to Secrets Manager → bump ProvisionerVersion → CFN deploy → Lambda re-invokes → Grafana updated

Auto-sync .env after deploy — added _sync_env_file() to deploy.py to update GRAFANA_WORKSPACE_ID and GRAFANA_ENDPOINT in .env after every successful deploy

Auto-sync .env after bootstrap — added _sync_env_file() to bootstrap_grafana.py — initially only synced when phase 2 ran in-process (token in memory), fixed to always read live values from CFN outputs + Secrets Manager so --phase 4 or --from-phase 4 also updates .env correctly

push_dashboard_secret() and bump_provisioner_version() — added to scripts/deploy_stacks.py, wired into deploy.py full deploy mode so python main.py deploy automatically handles the full dashboard publish pipeline

Permissions & Access

sso.auth.access-denied after deploy — workspace was created with zero users, needed to assign SSO user — discovered the id field requires SSO User UUID not email address

manage_grafana_permissions.py script — created to automate the 3-step SSO permission flow (get identity store ID → resolve UUID from email → call Grafana update-permissions), added as python main.py permissions subcommand

grafana-permissions.json removed — was a manual workaround for Windows shell quoting issues, made redundant by the permissions script

Bug Fixes

ProvisionerVersion auto-increment — bumped manually multiple times during testing (11 → 30) as each --deploy run increments it; this is now automated

io import missing in deploy.py — Pylance flagged reconfigure on TextIO — fixed by adding import io and using isinstance(sys.stdout, io.TextIOWrapper) for proper type narrowing

mypy duplicate module error — grafana/scripts/build_dashboard.py found twice as build_dashboard and grafana.scripts.build_dashboard — fixed by adding explicit_package_bases = true to mypy config in pyproject.toml

deptry DEP001 on grafana import — local module import in deploy.py flagged as missing package — fixed with per_rule_ignores = {"DEP001" = ["grafana"]} in pyproject.toml

Documentation

developer-guide.md overhauled — removed all manual aws cloudformation deploy commands, rollback raw CLI, duplicate build/validate sections — replaced with Quick Reference table, --deploy hook documentation, dashboard publish pipeline flow diagram, ProvisionerVersion explanation, auto-sync .env docs

Terminology clarified — established clear distinction between update (edit row file), build (assemble JSON), publish (push to Grafana), deploy (push CFN stacks) — important because "deploy" was being used loosely for all four

Commit discipline established — agreed to only commit after verifying changes work in Grafana — no point committing broken dashboard changes, test first with build_dashboard.py --deploy, verify in browser, then commit


=================

2026-05-15

- Lambda error-rate panel was taking forever to load due to 2 calls (errors/invocations), causing the whole dashboard to freeze - a major slow-down for other tasks
- Lambda error panels — removed Error Rate, paired Throttles + Concurrency
- Concurrent Executions panel to mirror the SQS format
- Fixed all Lambda panels to use auto period, Throttles as timeseries, added noValue
- renamed Lambda functions to lda- prefix, moved infra/lambdas/ to lambdas/
- Added cfn-lint as a library, updated version notes, fixed execution role permissions
- Added $platform variable to ECS panels — introduced a template variable so all ECS panels can be filtered by platform name, matching the pattern used in SQS/Lambda rows.
- gridPos fix for rows 04–10 — corrected panel positioning in the ECS row (and others) after structural changes upstream.
- Removed the ECS Container Insights Lambda entirely; becasue we want only free of cost things for PoC — deleted the 
ecs-container-insights.yaml
- SQL migration + per-service breakdown - the biggest change. Migrated all ECS panels from basic CloudWatch metric queries to Metric Insights SQL syntax. Added per-service breakdown so each ECS service gets its own series in the panels. Fixed Panel 4's metric (likely CPU/Memory).
- Removed SCHEMA() syntax — the initial SQL used SCHEMA(...) which hit CloudWatch query limits. Replaced with FROM "ECS/ContainerInsights" namespace syntax to avoid the error.
- Split multi-query panels + removed blank labels — panels that had multiple metrics in one query were split into separate targets for clarity. Removed empty legend labels that were showing as blank in Grafana. Provisioner bumped to v71.
- Added filterFieldsByName transformation — added Grafana field transformations to filter results by the meridian platform name, so only relevant ECS services appear.
- Switched from hardcoded "meridian" to $platform variable — replaced the static string with the dashboard variable so the filter works dynamically across environments.
- Final deploy — merged multi-platform changes and deployed the completed ECS row with the $platform variable filter active. Provisioner settled at v69.

=================


Table:=
===============================
Name | Number | Region | Env
===============================

Name should be clickable - So that it can directly go to the AWS resource.



- throttling limit for lambdas 
- how we know if this ECS is API or Queue Listner: ALB

SLO - SLI - SLA => By observing API
Auto Scaling config, is it enough;

Involve; Jin once we have unified dashboard.


=============