# May 4-8 Bhavik RPA Support

week1 of RPA support for me - Bhavik
April 2026-04-13 Monday

My presentation feedback - it was good start
Add to JIRA SRE - add Three More Tags: 1) BPM; 2)Citco works; 3) Meridian

Error logs - on dashboard - data are not proper yet

Tags needed: 
1) Meridian; 2) IA-BPO; 3)RPACFS1 4) BPM 5) Citcoworks 6) Automationhub

by. lamda
bars Export in excels - or as csv

Performance: Top 10 landa

Health Dashboard: Dashboard

Confluence Page for - lamda list

===================

# Overview
## RPA Support:=

### Blue Prism - BPM
1) E-binder
move close - check status

RPAsite we handle -  RPA we process - Daily;Monthy;Quarterly
add new bots if needed

2) VPL check:= frequency 
tight deadlines - because of closing stack price. 

@5 PM EST; Start Monitoring.
+ 3 bots handling the job
+ 3 more bots of

5 PM EST managing queue

3) Make Notes from Confluence Page
Corporate Action processes:
- Mandatory
- Voluntary
- Op Transfer
- Two izma ops trasfers.

===================

# RPA Support 

They can add 1 job a time Limitation; Longest job 30 min

Migration Trackers:
## Blue Prism -> UI Path or AWS 
## UI Path 
Cloud - Most of RPA migrated here
Non-Cloud (aws-vpc) ~ On-Prem - FA-Audits

Where is it (RPA) -> Cloud or On-Prem


UIPath Orchestrator <-> API Calls <-> DB (MySQL Server)
Runtime - Nodes ~ Machines (EC2 instances)
Nodes (1, 2, 3,..., 50) - could be more than 50 nodes

Citco Infrastrutues is where runtime machines runs.

Runtime -> Note -> Heartbeat -> Orchestrator
                     |
                     |
                     V
                Status Update -> Events

Orchestrator: Schedule Triggers, API, SQS

Runtime / Node / Requests asks for instruction to follow. 
DB gives instructions
Process runs on our Infrastrutues

Blue Prism -> Pool (Group of resources) (Node 1 + 2 group; Node X/Y group)
UI Path -> Template

UI Path -> Folders 
Example of folders: CoE (Center of Excellence); FA DataOps; etc
- Foldes in UI Path; have processes; assets; runtime (machines); queue
- DB check processes to run
- We run processes on machines (runtime) (manually; or Scheduled; etc)
- Foreground processes can be bloacked; blackground process wont be blocked
- Logs to troubleshooting

- every request is licensed. Multithreaded process can run on multiple threads
- we are trying to migrate processes from Blue Prism (old) to UI Path.

====================
  
# Compute & File Consolidation Notes — Training Week Summary  
  
## 1. Compute Service Limits  
  
| Service | Key Limitation |  
|---|---|  
| **AWS Lambda** | Max **15 minutes** per invocation (hard limit) |  
| **EC2** | No hard execution timeout — can run long tasks (e.g., 2+ hours) if needed |  
| **ECS Fargate** | Task provisioning/cooldown delay — if code doesn't finish within expected window, task may need to be manually killed |  
  
Fargate Cooldown Issue:**  
- Fargate has a startup/scaling delay ("cooling period") when provisioning new tasks.  
- If a task's processing logic doesn't account for this timing, the task may hang or fail to complete — requiring manual termination.  
  
---  
  
## 2. File Consolidation Process — Memory Issue  
  
### Symptoms  
- Process consumes **excessive memory**  
- Consolidation task doesn't complete cleanly → must be killed  
- Memory usage increases as more files are consolidated  
  
### Guidelines / Fixes  
- **Minimum memory**: 8 GB  
- **Instance family**: Use **R6 or above** (memory-optimized instances)  
- **Limit concurrency**: Process only **2–3 requests at a time**  
- **File size exception rule**:  
  - If an **individual file exceeds ~100–200 MB** → **do not consolidate**  
  - Send these as **standalone CSV files** instead  
- **Exception list files** → also skip consolidation, send directly  
  
### Report Type Reference — "Box Posision "  
- **Box position** refers to reports such as:  
  - **Position reports**  
  - **Cash reports**  
  - Other **box-level reports** (~600 records per report)  
- Format: **CSV**  
- Approx. size threshold discussed: **~494 MB+**  
  
### Output Handling  
- For files that skip consolidation:  
  - Export as **raw CSV**  
  - Drop directly into the designated **CSV output folder** ("CSK" folder — team-specific naming)

---  
  
## 3. Summary Rule of Thumb  
  
> - **Consolidate** only when individual files are small (< 100–200 MB).  
> - **Skip consolidation** for large files or exception-list items — send as standalone CSVs.  
> - **Use R6+ instances**, minimum 8 GB memory, to handle consolidation memory load.  
> - **Limit concurrent consolidation jobs** to 2–3 at a time.  
> - **Monitor Fargate task runtime** — build in handling for cooldown/provisioning delay to avoid killed tasks.  
> - Route large/exception files directly to the **CSV folder** rather than through consolidation.



==============================

## Blue Prism Folder Structure (PROD vs UAT)
*(From notebook screenshots — Apr 13 training)*

### PROD Environment — Folder Hierarchy
```
COE
└── DataOps
    ├── Processes (P1 – P20)
    │   └── M1, M2, Mc (machines)
    ├── Assets
    ├── Rules (Runtime) — M1, M2, M3
    └── Queues
```
- UAT = **similar to PROD** in structure
- **Logs** to troubleshoot issues
- **Mostly foreground** during dev time
- Execution modes:
  - **Foreground** → blocks the bot machine
  - **Background** → it will NOT block (non-blocking execution)

---

## Blue Prism Licensing & Multithreading

- **Every robot/node is licensed** → Background mode is expensive per license
- **Multithreaded** Blue Prism processes:
  - A single **Process** can run with multiple threads (Thread 1, Thread 2 ... Thread 20)
  - Enables parallel execution within one process
- **Migration direction**: AS processes from **Blue Prism → UIPath** (Cloud + On-prem)

---

## Aexeo Reporting Architecture (COMAPI / REST API)
*(From notebook diagrams + Confluence PDFs)*

### Overview
The **Reporting Server (Service)** exposes a REST API wrapper around COMAPI:

```
Python API calling ComAPIs
         ↓
   [Wrapper / REST API]
         ↓
  ┌──────────────────────────────────┐
  │  Aexeo Reports (ComAPI)          │  ← ComAPI
  │  P&L            (ComAPI)         │  ← ComAPI
  │  A/C Manager    (ComAPI)         │  ← .NET application
  └──────────────────────────────────┘
         ↓
   RDS / PostgreSQL
```

- **COMAPI = technology-dependent** — cannot be called via standard HTTP
- Each LOB has its own COMAPI process running on **Blue Prism**
- **JS → multiple requests to ComAPI on a single rule**
- Python wraps these via the Reporting Server REST API

### COMAPI Libraries (from Confluence PDFs)

#### 1. Aexeo Active PnL - COMAPI (`COMAPI-090826-030119`)
- **Library**: `IA_Aexeo.ActivePnl.ComApi` v1.0.3 | **Jira**: IA-7727 | **Developer**: Daniel Estrella
- **Flow**: Receives Report Criteria → Creates JSON Payload → Invokes `ActivePnLComApi.js` → Waits for report → Saves to UiPath Share
- **Pre-requisite**: `ActivePnLComApi-2021.08.421.00.Installer.exe` must be installed on the bot machine

**Arguments:**

| Argument | Direction | Type | Required | Description | Default/Sample |
|---|---|---|---|---|---|
| `in_dt_ReportCriteria` | In | DataTable | Y | Report Criteria for P&L | — |
| `in_ClientName` | In | String | Y | Name of Client in Aexeo | `CS_HEDGING_GRIFFO4` |
| `in_FileName` | In | String | Y | File name without extension | `CS_HEDGING_GRIFFO4_ActivePnl_YYYYMMDD` |
| `in_SaveLocation` | In | String | Y | Directory to save report | UiPath Share path |
| `in_ComApiTimeout` | In | Int32 | N | Timeout in **minutes** | `60` |
| `in_ComApiGridviewType` | In | String | N | Grid view type | `"global"` or `"client"` |
| `in_ComApiSaveCsvMethod` | In | String | N | CSV save method | `SaveAsRawCSV` or `SaveAsCSV` |
| `out_ComApiMessage` | Out | String | N/A | Message from COMAPI run | — |
| `out_bool_ComApiSuccess` | Out | Boolean | N/A | Success flag | `True` / `False` |

**Orchestrator Assets:**

| Asset Name | Type | Description |
|---|---|---|
| `Aexeo.ActivePnl.ComApi.UiPathShare` | Text | UiPath Share Drive root |
| `Aexeo.ActivePnl.ComApi.Timeout` | Integer | Default API timeout |
| `Aexeo.ActivePnl.ComApi.Script` | Text | JavaScript used for COMAPI |
| `Aexeo.ActivePnl.ComApi.Credentials` | Credentials | Encoded credentials for Aexeo |

**Directories on UiPath Share:**
- `\LOGS` — one log file per execution (`_COMPLETED_` or `_FAILED_`)
- `\REQUESTS` — request files (`.end`, `.json`, `_ErrorLog.err`)
- `\SCRIPTS` — contains `ActivePnlComApi.js`
- `\OUTPUTS` — generated report output files

---

#### 2. Aexeo Report Generation - COMAPI (`COMAPI-090826-030241`)
- **Library**: `AexeoReporter-COMAPI` | **Developer**: Nova Katrina Gomez
- **Flow**: Build JSON payload → Invoke `ReporterAPIv1.0.8.JS` → Wait for report → Move to save location → Retry 3x on `.err` files
- **Pre-requisite**: `AexeoReporterComApi-2021.08.421.00.Installer.exe` must be installed; `COMAPI JavaScript` at `\Scripts\AEXEO_COMAPI\ReporterAPIv1.0.8.JS`

**Arguments:**

| Argument | Direction | Type | Required | Description | Default/Sample |
|---|---|---|---|---|---|
| `in_ClientName` | In | String | YES | Client name in Aexeo | `WT_CAPITAL` |
| `in_FileName` | In | String | YES | Report filename | `WT_CAPITAL_RPA_11400_yyyyMMdd` |
| `in_ReportID` | In | String | YES | Aexeo Report ID | `100011400` |
| `in_GridviewType` | In | String | NO | Gridview type | Default: `Global client` |
| `in_SaveLocation` | In | String | YES | Save location path | UiPath Share path |
| `in_dt_ReportCriteria` | In | DataTable | YES | Customized report criteria | — |
| `in_APICredentialsName` | In | String | NO | Orchestrator credential asset | `"Aexeo_COMAPI_Credential"` |
| `in_AexeoCOMAPITimeout` | In | Integer | NO | Timeout in seconds | `300` (default 3000s/50min) |
| `in_AexeoComapiInvokeCode` | In | String | NO | Custom COMAPI JS path (overrides default) | `ReporterAPIv1.0.8.JS` |
| `in_blnWaitReportPerFund` | In | Boolean | NO | Wait per fund (only if custom JS used) | `True` |
| `out_ReportCriteriaJSON` | Out | String | N/A | Report criteria JSON (audit log) | — |
| `out_blnSuccessfullyDownloaded` | Out | Boolean | N/A | Download success flag | — |

**Orchestrator Assets:**

| Asset Name | Type | Description |
|---|---|---|
| `Aexeo_COMAPI_Credential` | Credential | Username + password for Aexeo |
| `AexeoReporter_COMAPI_Timeout` | Integer | Timeout threshold (default 3000s / 50min) |
| `Env_Root_SharedFolder` | Text | Root folder path |

**Sample JSON Payload:**
```json
{
  "Value date": "20240405",
  "Gridview": "AEXEODB_11400",
  "Fund": "",
  "bG9naW51c2Vy": "username",
  "ZW5jcnlQYXNz": "password",
  "Save Location": "\\\\uipath-file-share...\\SOURCE\\",
  "ReportID": "100011400",
  "File Name": "WT_CAPITAL_RPA_11400_20240405",
  "Client": "WT_CAPITAL",
  "GVType": "global"
}
```

> **Key difference vs Active PnL**: Report Generation also accepts `in_AexeoComapiInvokeCode` (custom JS) and `in_blnWaitReportPerFund`, and outputs `out_ReportCriteriaJSON` for audit logging. Active PnL is specifically for P&L reports and uses named `in_ComApiTimeout` in **minutes** (not seconds).

---

#### 3. Aexeo Reporter Server - HTTP Request (`090826-030459`)
- **Library**: `IA_AexeoReporterServer_HTTP_Request` | **Developer**: Nova Katrina Gomez
- **Flow**: Build JSON payload → HTTP POST to Aexeo Reporter Server → Wait for report → Download to UiPath Share
- **Script**: `reporter_requests.py` at `\Scripts\AexeoReporterServer-HTTPRequest\`
- **Pre-requisite**: `windowsdesktop-runtime-6.0.0-win-x64.exe`; `pip install requests`
- **Enhancement (Jan 2025, IA-11432)**: Added `in_SourceSystem` optional param to track which process called the API

**Arguments:**

| Argument | Direction | Type | Required | Description | Default/Sample |
|---|---|---|---|---|---|
| `in_ClientName` | In | String | YES | Client name in Aexeo | `WT_CAPITAL` |
| `in_FileName` | In | String | YES | Report filename | `WT_CAPITAL_RPA_11400_yyyyMMdd` |
| `in_ReportID` | In | String | YES | Aexeo Report ID | `100011400` |
| `in_GridviewType` | In | String | NO | Gridview type | Default: `Global client` |
| `in_SaveLocation` | In | String | YES | Save location (must be UiPath Share) | UiPath Share path |
| `in_dt_ReportCriteria` | In | DataTable | YES | Customized report criteria | — |
| `in_AexeoReporterServerTimeout` | In | Integer | NO | Timeout in seconds | `300` (default 3000s/50min) |
| `in_APICredentialsName` | In | String | NO | Orchestrator credential asset | `"Aexeo_COMAPI_Credential"` |
| `in_SourceSystem` | In | String | NO | Which process called the API (audit) | `RECON_EBINDER` |
| `out_blnSuccessfullyDownloaded` | Out | Boolean | — | Download success flag | — |

**Orchestrator Assets:**

| Asset Name | Type | Description |
|---|---|---|
| `Aexeo_COMAPI_Credential` | Credential | Username + password |
| `AexeoReporterServer_HTTPRequest_Timeout` | Integer | Timeout (default 3000s/50min) |
| `Env_Root_SharedFolder` | Text | Root folder |
| `AexeoReporterServer_HostURL` | Text | Reporter Server endpoint URL |

**Sample HTTP Request URL:**
```
https://rpacfs1-aexeoreporterservice-uat.int.coreprod01.citcosvc.com/arguments?argument=
{"Start Date":"20240510","End Date":"20240510","Gridview":"AEXEODB_11400",
 "bG9naW51c2Vy":"username","ZW5jcnlQYXNz":"password",
 "Save Location":"\\\\server\\BPShare\\","ReportID":"100011400",
 "File Name":"EIP_RPA_11400_20240405","Client":"EIP","GVType":"global"}
```

> **Key difference vs COMAPI**: HTTP Request uses the Aexeo **Reporter Server REST endpoint** (no local COMAPI install needed beyond the runtime). Reports can **only** be saved to UiPath Shared Drive. The `in_SourceSystem` field was added in Jan 2025 for process-level audit tracing.

#### Library Comparison Summary

| Feature | Active PnL COMAPI | Report Generation COMAPI | HTTP Request |
|---|---|---|---|
| Reference | `030119` | `030241` | `030459` |
| Mechanism | Local COMAPI JS | Local COMAPI JS | REST HTTP POST |
| Timeout unit | **minutes** (default 60) | **seconds** (default 3000) | **seconds** (default 3000) |
| Retry on `.err` | No (own logic) | Yes — 3x retries | No |
| Audit JSON output | No | Yes (`out_ReportCriteriaJSON`) | No |
| `in_SourceSystem` | No | No | Yes (added Jan 2025) |
| Custom JS override | No | Yes (`in_AexeoComapiInvokeCode`) | No |
| Save location | UiPath Share | UiPath Share | UiPath Share only |

---

## Reporting Service Architecture — Multi-Client Design

### Key Constraints
- **Each client has their own DB(s)** — databases cannot be shared across multiple clients
- **ComAPI is client-specific** — tech-dependent, cannot call via plain HTTP

### Architecture Flow
```
Central Node Master
        ↓
   API Gateway
        ↓
  Reporting Service
  ┌───────────────────────────────┐
  │   API  ←→  Processors         │
  │            ↓                  │
  │          Workers              │
  └───────────────────────────────┘
        ↓           ↓
       SQS      COM APIs:
              ┌──────────────┐
              │ ComAPI Axeo Rp│
              │ ComAPI P&L    │
              │ ComAPI A/CMgr │
              └──────────────┘
```

### Scaling / Threshold
- **Threshold**: 25 / 50 / 75 (small → M)
- Processes **1 page at a time** for large reports
- **2000 requests can be handled by one EC2 instance**
- Report sizes: up to ~572 MB → 100 records → EC2 instances (205 MB, 25 MB, ECS)
- Save output to: **shared drive + S3 bucket**

---

## GridView (ComAPI Feature)

- **GridView is supported by ComAPI**
- UI calculates the things that need to be displayed
- **MESO / EBinder client reporting uses GridView**
- Related concepts:
  - **Throttling** — controls request rate to ComAPI
  - **DB instance type** — affects throughput

### MESO Job State Machine (from notebook)
```
accepted → pending
               ↓
           running
               ↓
      complete | fail (outcome)
```
- ★ **Less than threshold** → release (process normally)
- **Otherwise** → push back to queue (FIFO or any clever mechanism)
- **Workers**: 2-hour timeout
- Uses **Axios** for HTTP calls + logging

---

## ECS Fargate & Deployment (MESO Infrastructure)
*(From notebook screenshot)*

### ECS Fargate Scaling Challenge
- **5-minute cooldown** period on scale events
- Trigger: **SharePoint APS Task** → if **>60% CPU cap** → scale up ECS (1–5 instances)

### CI/CD Pipeline
```
Jenkins → Build
              ↓
    Ansible Script (deploy on machine)
              ↓
            Linux
```

### Running Python as a Service
- **NSSM**: Python as a **Windows Service**
- Web server options:
  - **Apache** (enterprise)
  - **nginx** (free, limited support)

---

## MESO Support Documentation (`090826-031120`)

### Process Information
- **Process Name**: MESO (Middle Office Month-End Sign-Off)
- **Technology**: AWS & Python
- **AWS Environment**: `citcocoreprod01` | **Region**: `us-east-1`
- **Code Repository**: `ia_meso_process` — https://bitbucket.org/citco/ia_meso_process/src/main/
- **Pipeline**: `core-ia-bpo-meso-service-infrastructure` (eu-west-1 CodePipeline)
- **User Guide / Trigger File**: `MESO_Single_Trigger_Process_V5.1_TEST.xlsm` (TEST environment reproduction)
- **Event Codes Reference**: `event_codes_summary.csv`

### Team
| Role | Person |
|---|---|
| Process Developer | Ho, Hoi Leung; Deotale, Kishor (Ali, Mir Momin — deactivated) |
| Support DL | `innovation_it_support` |
| Aexeo Support — DEV | Feng Cao / Jajoo, Saurabh |
| Aexeo Support — NA | Sehgal, Mandeep |
| Aexeo Support — MNL | Halili, Anna Marie |
| Aexeo Support — EU | Howard, Kees |

### Storage Locations

| Type | UiPath ShareDrive (legacy) | S3 (current) |
|---|---|---|
| Positions output | `\\uipath-share...\RECON_EBINDER\MESO\OUTPUT\POSITIONS` | `s3://prod-citco-ia-checklist-service-documents/meso/output/position/` |
| Cash output | `\\uipath-share...\RECON_EBINDER\MESO\OUTPUT\CASH` | `s3://prod-citco-ia-checklist-service-documents/meso/output/cash/` |
| Config | `\\uipath-share...\RECON_EBINDER\MESO\CONFIG` | `s3://prod-citco-ia-checklist-service-documents/meso/config/` |
| SharePoint output | `http://citcoportal/cfs/sites/sharedservices/FTB/.../Manila/SSC Shared/Middle Office` | — |

### External Dependencies
1. RPA Generic Trigger Lambda
2. Aexeo Reporting Server
3. Aexeo (application)
4. Citco Recs DB
5. SharePoint
6. UiPath ShareDrive

### MESO Configuration (SharePoint)
- **MESO Config schedules** are managed in SharePoint lists:

| Schedule | SharePoint List URL |
|---|---|
| Daily | `http://cfstoday/.../Lists/Meso%20Config%20Daily/AllItems.aspx` |
| Weekly | `http://cfstoday/.../Lists/MESO%20Config%20Weekly/AllItems.aspx` |
| Monthly | `http://cfstoday/.../Lists/MESO_CONFIG_MONTHLY/AllItems.aspx` |
| Quarterly | `http://cfstoday/.../Lists/MESO%20Config%20Quarterly/AllItems.aspx` |
| PreMESO | `http://cfstoday/.../Lists/Premeso%20Config_1/All%20Items.aspx` |

- **Grant Config Read/Write Access** (Supervisor/Manager level only): raise SD Ticket to RPA Support (coordinate with Hayden, Edmund / Zhang, Jiannan)
- **Client Onboarding**: User's Supervisor/Manager responsible
- **Add Funds to existing config**: Supervisor/Manager OR SD Ticket to RPA

### Pre-Month End Activities

**1. Pre-Month End Activities (Cash):**
1. Manual_GL_Entries_Report_(100216300)
2. B-3 Cash ME Signoff Backup
3. B-1.2 Cash Rec
4. B-1.3 Aexeo Cash Completeness
5. Broker ART Extract
6. Badly Entered Trades Report (100123200)
7. C-3 Pos ME Signoff Backup (positions)
8. Boxed Position Report (100402900)
9. Exception_Manager_Report
10. C-1.2 Positions Rec
11. C-1.3 Aexeo Position Completeness

**2. Aexeo Reports (Cash):**
- Manual_GL_Entries_Report_(100216300)

**2. Aexeo Reports (Positions):**
1. Boxed Position Report (100402900)
2. Badly Entered Trades Report (100123200)
3. Deal Manager Check (100301200)
4. Expired Positions Report (10019800)
5. Order Manager Check (100301100)
6. Outstanding Paydown Check (100301300)

**3. Other Items:**
- Cash: Lock_Status_Report
- Positions: Exception_Manager_Report

**4. Macros (Cash):** C-4 Subscription and Redemption Template, B-3 Cash ME Signoff Backup, B-1.2 Cash Rec, B-1.3 Aexeo Cash Completeness, Broker ART Extract
**4. Macros (Positions):** C-3 Pos ME Signoff Backup, C-1.2 Positions Rec, C-1.3 Aexeo Position Completeness

**5. E-Binders (Cash):** B-1.3 Aexeo Cash Balance Sheet by Broker (CSV/PDF), B1_100221500_Cash_Recon, B-1.2 Cash Rec
**5. E-Binders (Positions):** C-1.3 Aexeo Position Report by Broker (CSV/PDF), C1_PNL_Position_Recon, C-1.2 Positions Rec

### MESO Database Details
- **DB Type**: PostgreSQL
- **Read-only user**: `meso_read_only_user` (Password: contact Clarisa / Horace / Kishor)

| Environment | JDBC Connection String |
|---|---|
| DEV | `jdbc:postgresql://dev-navchecklist-db.ckxyrig1s7l3.us-east-1.rds.amazonaws.com:5432/postgres` |
| TEST | `jdbc:postgresql://test-navchecklist-db.c8qd5xwqqyv1.us-east-1.rds.amazonaws.com:5432/postgres` |
| PROD | `jdbc:postgresql://rpacfs1-navpack-prod-db.int.coreprod01.citcosvc.com:5432/postgres` |

**Key Tables:**

| Table | Usage | Retention |
|---|---|---|
| `meso_request` | Tracks all MESO requests, process status, and timing | 7 days |
| `meso_clients_csv_reports` | Config for large-volume clients/funds — generate All Pending Report for last 3 months only | No removal |

**Process status values**: `PENDING` (in progress), `FAILED`, `COMPLETED`

**Useful SQL Queries:**
```sql
-- Check most recent 10 MESO requests
SELECT id, client, fund, start_date, end_date, schedule, run_type,
       required_reports, status, created_on, modified_on,
       (modified_on - created_on) AS "Elapse Time", notes
FROM meso_request
-- WHERE client in ('BRILLIANCE_CAPITAL','TALISMAN')
ORDER BY created_on DESC
FETCH FIRST 10 ROWS ONLY;

-- Check large-volume client config
SELECT * FROM meso_clients_csv_reports;

-- Kill/retrigger a stuck request (requires R/W access)
UPDATE meso_request SET status = 'FAILED' WHERE id IN ('<id>');
```

### Event Store Registration
- **EVENT_SOURCE**: `RPA`
- **EVENT_SUBJECT**: `RECON-MONTH-END-SIGN-OFF-032024`
- **EVENT_AREA**: `INTELLIGENT_AUTOMATION`
- **Tenant ID (TEST)**: `7358631a-97bd-4bde-b13c-98d8979b9f10` (`GENERIC_UAT_NON_PROD`)
- **Tenant ID (PROD)**: `bb5919ad-4356-4240-a5a9-8577a2b9a848`
- **Process Identifier**: `RECON-MESO-TRIGGER`
- **Event Code example**: `RMES-I-0007` → `PROCESS COMPLETED`

> **Note**: TEST Event Store POST no longer works — TEST DB no longer saves event store messages. Use PROD endpoint only.

**Query Event Store (PROD):**
```
POST https://prd-events-service.eu-west-1.meridian.int.coreprod01.citcosvc.com/event/list_active
```
```json
{
  "tenant_id": "bb5919ad-4356-4240-a5a9-8577a2b9a848",
  "query": {
    "metadata": {
      "event": { "subject": { "op": "eq", "value": "RECON-MONTH-END-SIGN-OFF-032024" } },
      "ids": { "run_id": { "op": "eq", "value": "<run_id>" } }
    },
    "sort": [{ "collection_datetime": { "order": "desc" } }],
    "pagination": { "page": 1, "limit": 500 }
  }
}
```

**Sample Event Store response fields**: `uuid`, `knowledge_datetime`, `type`, `area`, `subject`, `source`, `code`, `message`, `run_id`, `client_name`, `fund_name`, `value_date`, `process_identifier`, `run_sequence`, `status`, `remarks`

### CloudWatch Logs
- **Log Group**: `/ia/meso/shared/worker`
- **CloudWatch Link (PROD)**: `https://eu-west-1.console.aws.amazon.com/cloudwatch/home?region=eu-west-1#logsV2:log-groups/log-group/$252Fia$252Fmeso$252Fshared$252Fworker`

**How to investigate a MESO issue:**
1. Connect to MESO DB → find the `id` of the request (Cash/Positions) using SQL above
2. Login to AWS → CloudWatch → Logs Insights
   - Query Scope: `/ia/meso/shared/worker`
3. Trace logs by request ID:
```
fields @timestamp, @message
| filter @message like '<request-id>'
| sort @timestamp desc
| limit 10000
```
4. Filter by multiple criteria using AND/OR:
```
fields @timestamp, @message
| filter @message like /(?i)PRISM/ AND @message like /(?i)RENDAO/ OR @message like /(?i)enunez/
| sort @timestamp desc
| limit 100
```

### RPA LLM Support Agent Prompts
Use the RPA LLM Support Agent to check MESO status without querying the DB directly:
1. *"What is the status of RECON MESO for the client `<Client Name>` Fund `<Fund Name>`?"*
2. *"Can you give the run id associated with this run?"*
3. *"Can you share more details along with data for each activity associated with this run id?"*

### AWS SSO Access
```
https://sso.citco.com/affwebservices/public/saml2sso?SPID=urn:amazon:webservices
```

| Environment | Shared Account Role | Core Account Role |
|---|---|---|
| TEST | `CTM-Developer@citcoshareddev01 (221048274280)` | `CTM-Developer@citcocoretest01` |
| PROD | `CTM-DevOps@citcosharedprod01 (857871466570)` or `CITCO-ReadOnly@citcosharedprod01` | `CTM-DevOps@citcocoreprod01` or `CITCO-ReadOnly@citcocoreprod01` |

### Error Scenarios & FAQ
- Full FAQ documented on Confluence: **MESO FAQ - Intelligent Automation**
- `event_codes_summary.csv` contains all MESO process event codes

==============================

> **Note on MESO Query Complexity:** LOB = Line of Business. Query complexity for MESO is too high to use OPM — must use native SQL queries instead.

---

## 2026-04-14 (Tuesday) — CitcoWorks KT (Bhanu Teja Murari)
*(KT delivered by **Bhanu Teja Murari** — IT Developer, Innovation IT, Hyderabad (HYD1), IN2840. Email: BMurari@citco.com)*

### CitcoWorks Overview
**CitcoWorks** (formerly Meridian) = Fund Administration platform for A/C management, Loans, and Client Product workflows.
- Solves **business workflows** across fund admin
- **Meridian Platform** (layered architecture)

### Meridian / CitcoWorks — Layered Architecture

```
┌─────────────────────────────────────────┐
│  L1: Product (SLAM, etc.)               │  ← Top
│  L2: Process                            │
│  L3: Sub-process                        │
│  L4: Process Task                       │
│  L5: Sub-task / Task item               │  ← Bottom
└─────────────────────────────────────────┘
```

#### Foundation Layer — Knowledge Graph (KG)
- **Knowledge Graph** is the backbone of CitcoWorks
- Stores all **relationships**: Products → Processes → Tasks → Sub-tasks
- Each item has: `attributes`, `attached fund`, `dependencies`, `optionals`
- KG is **synced from Optics** (source of truth for process/task structure)
  - Sync is **on-demand** (confirmed) — not fully real-time
  - Each **Friday**: team receives email and loads workflows into the KG
- **Who decides the hierarchy?** KG Loader — decides what is a subprocess vs sub-task
- **How are relationships added?** Via the KG loader sync from Optics
- **Process / Sub-process, Task / Sub-task hierarchy** decided by the KG Loader

#### Knowledge Graph Layer — Communication
```
Knowledge Graph
       ↓
Communication Layer
       ↓
   Event Service  ←→  Event Routing Service
       ↓
  Tenant (B.U.)
  Services
       ↓
  Mapping Tables
  (Area → Subject → Source → config for this system → Action Processors)
       ↓
  Code / Downstream System
  → performs some action based on events
  → Business Logic (other apps: MESO)
```

- **Observer pattern**: Event Service observes system attribute notifications
- **Event Routing Service** routes events to the correct **Action Processor (AP)**
- Each mapping row specifies: `Area`, `Subject`, `Source`, `Type`, `Action Processor`

### CitcoWorks — Action Processor & Event Routing

**In CitcoWorks, the Action Processor (AP):**
- Solves business logic based on event trigger routing
- AP can be anything that needs to be done in response to an event
- Template: update task status based on event → `Route (update status of task)`

**Scheduler:**
- Runs on **15-minute interval**, pushing/scheduling events
- Event Service **stores events** (currently a "dumping ground" — all events accumulated)
- Lot of events → API pulls list of active events from **DynamoDB**
- Where is the mapping hosted? → API, used by routing service

**Event Service:**
- Emits events to Event Routing Service
- Where does Event Service store events? → **ODL / Redis Elasticache → Kafka Q**
- The role of Event Service: observer → notifies downstream based on triggers

### Event Routing Service — API Structure

**Event Routing Service (ERS)** has multiple CRUD APIs:

| API | Purpose |
|-----|---------|
| `GET event/{uuid}` | Get event by ID |
| `POST add-route` | Map a downstream system |
| `list-tenant-routes` | List all routes for a tenant |

**Route configuration fields:**
- `consumer_id (TID)` — ID of the downstream system consumer
- `route name`, `event` (area/type)
- `advanced matching` — filter which data goes to the downstream queue
- `instance-type`, `domain`

**Filters:** `metadata.ids` — TID of router (consumer) stored somewhere?

**Downstream System (AP):**
- Consumes via Action Processor (name, e.g., `handle_socl_check`)
- `dynamic attributes` — consumer (B) wants the data
- **Can routing service do a pass-through?** → **No** (confirmed): cannot pass through without mapping
  - w/o mapping: cannot be passed if data is missing in source field → sends null (blank, not error)
  - If advanced matching has missing data → **fails** (confirmed)

**APIs for downstream:**
- `set-active-vision`
- `list-tenant-routes(id, distribute, list of routes, A.P. that this tenant has)`

**Who is responsible for transferring message to AP?**
- Listens to **Kafka consumer** → messages pushed to Q
- Event → Subject → Action Processor → Source → Type (SYSTEM) → metadata, clock, source-rpt → IDs (sub: MESO) → data → dyn_attrib (k/v pairs: client=data1, fund=data2, val=data3)
- **How event gets passed to AP** → through J routing
- **How routing service passes source event to AP** → via Kafka topic subscriber

### Event Service Internal Flow
*(From notebook diagram — Screenshots 1 & 2)*

```
Source System
      ↓  (event)
Event Service
      ↓
   Receive
      ↓
    Save  ──────────→  WHERE: ODL
                              Redis Elasticache
      ↓
Push to Kafka Q          ←── Open Q: "Kafka Q — name?"
    (or SQS)
      ↓
     End
```

**Storage on Save:** events saved to **ODL** and **Redis Elasticache** (confirmed from notebook)
**Transport:** pushed to **Kafka Q** (or SQS — still open: exact queue name unknown at time of training)

**What's behind Event Routing Service?**
*(From notebook Screenshot 3 — "routing, listener to Qs? Kafka")*

```
Kafka Q listener (R)
        ↓
       DB  ────────────── Q: "What is arch behind event routing service?"
        ↓                    (Bhanu Teja Murari — action required)
      [?]  ──→ WHERE: Action Processor listener
                        ↓
             Static Attrib (hard coded)
                        ↓
              ★ How does message reach Action Processors?
```

- **Routing service** = Kafka Q listener that reads from DB for route config
- **AP listener** listens to Kafka topic; AP process has **Static Attributes (hardcoded)** that define how to handle messages
- Open question at time of training: exact internal architecture behind ERS still being clarified with Bhanu Teja Murari (BMurari@citco.com)

### CitcoWorks Full Layered Architecture
*(From notebook Screenshot 4 — confirmed layer order)*

```
┌──────────────────────────────────────────────────┐
│  KG Layer         ✓ (confirmed)                  │
│  Communication Layer  ✓ (confirmed)              │
│       ↓                                          │
│  Execution Layer → Task Management               │
│       ↓                                          │
│  Collaboration Layer                             │
│    → Task Mgmt / Services                        │
│    → Business Action Workflow (registered here)  │
│    → Integration Services                        │
│       ↓                                          │
│  Supporting Layer                                │
│    1. Reference Data Service (Catalogue)         │
│    2. Config Service                             │
│    3. Task Assignment Service                    │
│    4. Scheduling Service                         │
│    5. Notification Service                       │
│    6. Interaction Service (text/attachment)      │
└──────────────────────────────────────────────────┘
```

- **Meridian app** sits at this layer (confirmed in screenshot)
- **Business Action Workflows are registered** in the Collaboration layer
- KG ✓ and Communication ✓ layers were explicitly confirmed during the session

**Notification Service:**
*(From notebook Screenshot 4)*
- Triggered when **other events need to be notified based on some "rules"**
- Rules-based: not all events → notification, only those matching configured rules
- Sends via: **email**, **SQS**, **SNS** (confirmed Apr 16 with Bhanu Teja Murari)

### User Onboarding to CitcoWorks
*(From notebook Screenshots 5, 8, 9)*

- Users get added through **Tenant Roles / Auth Master**
- User needs a **role** via Auth Master → role determines persona
- **"nav preparer"** = a persona with defined **Skill Sets** (e.g., SRE → Python, Monitoring)
- Not all users (out of e.g. 10) have the skill sets to fulfil the nav-preparer persona
- **Business** sends the info: which users have the skill set to do this persona
- **AuthMaster team**: Bhanu Teja Murari (BMurari@citco.com) knows a contact in Hyderabad — action required to get team details
- **Persona onboarding process**: tracked in CitcoWorks; confirm persona exists in both CitcoWorks and Optics
- **Process IDs**: AskCitco maintains a table of all process IDs
- ⚠️ Potential **onboarding problem**: if role/persona alignment is off, features may be incorrectly hidden/shown

### Task Management Service
*(From notebook Screenshots 5 & 6)*

**How tasks get assigned:**
```
task → assigned to → Preparer → by → task mgmt service
```

**Creating a persona:**
- Task Mgmt Service is where we **create personas**
- A persona has **standard skill sets** defined by the SRE team
  - e.g., SRE persona → skill sets: Python, Monitoring
- **Business people define the skill sets** (via template) — must be performed by that persona
- We **sync persona definitions to KG** using the task management service
- One persona → **multiple users** can do tasks based on task mgmt service

**User–Persona relationship:**
- `persona = usergroups = roles` — CitcoWorks does **not** use the concept of "roles" directly
- Multiple users share one persona; tasks are distributed across them
- Out of (e.g.) 10 users, **not all have the skill sets** to fulfil the (nav) preparer persona
- **Who grants skill sets?** → Business sends that info — specifying which users can fulfil this persona

**Feature visibility (hide/show):**
- Pages/features are **shown or hidden based on role**
- Open question: is hiding based on **role** or **persona**? → potential **onboarding problem** if misconfigured
- CitcoWorks roles are **static**: `user`, `tenant admin`, `read-only`
- Roles are **requested by business** and **defined by Auth-Master**

### CitcoWorks API Reference — Persona & User Management
*(From notebook Screenshot 7 — "C.P.: CitcoWorks API Reference")*

**How is a user added to a persona?**

```
AD (Active Directory)
        ↓
Task Assignment Service
        ↓
  POST create-persona       ← creates the persona definition
  POST management-persona   ← manages persona assignment
  POST manage-users         ← adds user → Citco → Optics IDs
        ↓
    role → persona
    (source: business / Optics)
```

**Key API endpoints (Task Assignment Service):**

| Endpoint | Purpose |
|----------|---------|
| `POST create-persona` | Create a new persona definition |
| `POST management-persona` | Manage persona (update, assign) |
| `POST manage-users` | Add/map users → Citco + Optics IDs |

**Persona model:**
- `persona = usergroups = roles` — CitcoWorks does not have a traditional "roles" concept
- Users are grouped under a persona; persona is the unit of access
- Role data comes from **business** (who can do what) and **Optics** (source of truth for process hierarchy)

**Feature visibility:**
- Pages are **shown or hidden based on role**
- Q: Is hiding based on **role** or **persona**? → open question, potential **onboarding problem** if not aligned

### Auth Master — Login Flow & Architecture
*(From notebook Screenshots 9, 10, 11)*

**Login flow:**
```
Users
  ↓
Citco-Works UI
  ↓
Login
  ├── AD (Active Directory)
  └── Ping Identity → token for UI
        ↓
  Login Success
        ↓
  Meridian Auth Service  (our service)
        │
        ├── sends: user ID, app name, service name (C.W.)
        │
        └── real-time call to AuthMaster
                  ↓
            AuthMaster
              → returns: 1) persona
                         2) roles
        ↓
  Caching (C.W. service-name level)
```

**CitcoWorks roles (static — 3 types):**
- `user`
- `tenant admin`
- `read-only`
- Roles are **requested by business** and **defined by AuthMaster**

**Open questions / risks noted in session:**
- ⚠️ **Who is AuthMaster team?** → Bhanu Teja Murari (BMurari@citco.com) knows one contact @Hyderabad (action required)
- ❓ How is data synced from AuthMaster? Real-time? API-based? How frequently?
- ❓ How does Meridian API call AuthMaster — and how often?
- ⚠️ **If AuthMaster is down → users cannot perform any action** (critical dependency)
- ❓ TTL for cache — how long? How often is cache flushed?
- ❓ What happens if cache is flushed (e.g., on service restart)?
- ⚠️ **If a user is deleted in AuthMaster → what happens to the cache?** → potential problem (stale cache entry)
- ❓ Do users receive their persona on every login, or is it cached?

**Cache behaviour (what we know):**
- CitcoWorks caches persona + roles after login via Meridian Auth Service
- Cache is keyed at the service-name (C.W.) level
- Cache flush scenario (restart) — behaviour undefined at time of training

---

### Task Assignment Service & Task Lifecycle
*(From notebook Screenshots S12, S13, S14, S15, S16)*

**How tasks get assigned to users:**
```
Login Success
      ↓
Task Assignment Service
      ↓
  "My Tasks" view  (tasks allocated to this user)
      ↓
  Open / In-Progress
```

- Task Assignment Service assigns **new tasks to the least-occupied user** (load balancing)
- **What if a user is on vacation?** → **HIL (Human-In-the-Loop)** — Manager assigns to a group

**Task lifecycle — high-level (open → offered → completed):**
```
open → offered → completed
```

**Task lifecycle — detailed states (from notebook S14, S15):**

| Dimension | States | Description |
|-----------|--------|-------------|
| **State** (SRE / system internal) | `1. Initiated` | From KG, initiated into system (consistent/internal label) |
| | `2. Running` | Task is active and being processed |
| | `3. In-Exception` | Exception path triggered |
| | `3. No-Exception` | Normal path (no exception) |
| | `4. Completed` | Task done (both paths converge here) |
| **Status** (user-visible) | `Empty` | Task created, not yet assigned |
| | `Open` | User is assigned to task |
| | `In-Progress` | User is actively working |
| | `FOR-REVIEW` | Task needs review before completion |
| | `Failed` | Leads to exception state |
| | `DONE / Completed` | Successfully finished |

**Exception task creation (from S14):**
- When `In-Exception` state is reached → a **new exception task is created**
- The new task gets **type = exception**
- This exception task is separate from the original task — it has its own lifecycle

- **ADM** (admin view): tracks task creation and user assignment events separately
- **Clone** action exists on tasks — allows duplicating a task (use case: reprocessing / parallel handling)
- Task Assignment API drives all lifecycle transitions

**Task Interaction Service APIs (from S13):**
```
API  (task mgmt services)
        ↓
  POST submit_interaction/   ← user/event interaction for tasks
```
- `POST submit_interaction` = endpoint for submitting a user or event interaction against a task
- **User/event interaction** is used for tasks (e.g., submitting a response, attaching data)
- Two API surfaces confirmed (action required — verify with Bhanu Teja Murari):
  - **Task Interaction Service APIs** — handles task-level interactions
  - **Task Management Service APIs** — handles persona, assignment, lifecycle

---

### Event Store — Sequences (Discussion with Sridhar)

**Standard sequence:**
```
PROCESS_STARTED → IN_PROGRESS → COMPLETED → PROCESS_COMPLETED
```
- Why defined? → when to send event, include project code → tied to project name
- **Retrograding**: FX Closeout agent using UCM / IRA

---

### Event Service — Detail
*(From notebook S10, S11)*

**RPA world → Intelligent Automation team mapping:**
- RPA world uses `BE` (Business Exception) / `SE` (System Exception) codes
- Example: `Code 003` → area = who requested the change (e.g., AP)
- **Each process has a mapping** (boxed in notes — fundamental design principle)
- **1 process can have multiple events** (fan-out)
- Exception: we defined why BE / SE — when sending event, include project code → tied to project name

**Process IDs:**
- ★ **Where can we find all process IDs?** → `[Ask Citco]` = **we maintain a table** (internal registry)
- `[Event-store tool]` — tooling exists to inspect/query the event store

**Each process has an event code:**
- `send → payload` — IDs are defined here
- IDs are **searchable fields** in ODL / OpenSearch
- **Volume** = how many clients are processed (tracked per run)

**Life cycle of a trace:**
```
trace_id ──→ start ──→ middle ──→ end
```
- Each step completed → **events are sent to area**
- **Area changes** based on exception (dynamic routing on exception)
- `id` → process identifier: "this event is for which process?"

**Run ID → Sequences structure:**
```
Run ID
  ├── Messages  →  Stages       (what happened at each stage)
  ├── Status    →  Activity     (current activity state)
  └── Remarks   →  All details about the run
```

**ODL as Event Store:**
*(From notebook S8)*
- **All events are saved in ODL**
- Use **Event Service APIs** to get data from ODL (not direct DB access)
- Key identity: `event_subject = <project code>` — subject field IS the project code
- **Query mechanism**: OpenSearch-based, queried by:
  - `project code` (= `event.subject`)
  - `run_id`
  - `metadata` fields
- `message → (status – value)` — message field carries the status-value pair

**JIRA / LLM integration (future):**
- `LLM → latest event → run_id → all events` — LLM can create JIRA tickets from latest event + run_id context

**Event Service Dashboard (JIRA — future feature — Sridhar/team):**
*(From notebook S9 — "Retrograding Repo FX Closeout agent by using UCM / IRA")*

**Retrograding note:** FX Closeout agent retrograded using UCM / IRA — this triggers Event Store re-processing via filter across all tenants.

**Dashboard scope:** all tenants / per-tenant view, with UI filters (Sridhar / ODL team)

**Three core metrics (①②③ from S9):**

| # | Metric | Detail |
|---|--------|--------|
| ① | **How many projects injected** | Count of distinct projects sending events |
| ② | **# of events per project per day / month** | Event volume breakdown by project + time window |
| ③ | **How many projects missing completion** | Each run tracked via: `process → run_id → PROCESS_STARTED → PROCESS_COMPLETED` — flag runs that started but never completed |

- **Time to complete the RUN** — duration metric per run_id (PROCESS_STARTED → PROCESS_COMPLETED delta)
- All events saved in **ODL**
- Use Event Service APIs to get data from ODL
- Query by: `event.subject` (= project code), `run_id`, OpenSearch
- `Create JIRA → LLM → latest event → run_id → filter by project, client, dates → progress of event`
- **Dashboard phase 2**: # of actions taken, Monitor + Alerts, # of clients processed
- ODL access: given for **one tenant only** (multi-tenant access is a pending gap)
- **If no events for a process for a month** → flag as anomaly

### Exception Handling — Q&A (Training Session, Bhanu Teja Murari)
*(From notebook S12)*

**Q: If a task is in exception status — who is assigned to the exception task?**
- → **Persona** handles exception tasks
- Follow-up: what if the persona doesn't exist? → open question (no answer at time of session)

**Q: ADIC — do we have exceptions?**
- ADIC: **we don't have exceptions (manual)** — ADIC processes are handled manually, no automated exception flow

**Q: Axius exceptions — how does it work?**
- Axius Exception = **automated task** → event is triggered, **having a user** (user-attributed exception)
- Contrast with ADIC: Axius has automated exception events tied to a user context

**Q: In which scenario is an exception task created?**
- Either a **system exception** OR a **business exception**
- Exception task is created when: **event is of task-type = exception**
- Two triggers:
  - `system exception` — infrastructure/processing failure
  - `business exception` — rule/data validation failure
  - event's task type field drives which kind of exception task is created

**Q (Bhanu Teja Murari): What is about Time Card Generation & BPM flow?**
- Open question raised by Bhanu Teja Murari — Time Card Generation process and its BPM (Blue Prism) flow not covered in this session
- Action: follow up with Bhanu Teja Murari (BMurari@citco.com) / Horace on Time Card Generation process details

---

### Supporting Services Summary (CitcoWorks)

| Service | Role |
|---------|------|
| Task Management Service | Persona, skill sets, task assignment |
| Reference Data Service (Catalogue) | Reference data |
| Config Service | Centralized config per env |
| Task Assignment Service | Assign tasks to personas |
| Scheduling Service | SLAs calculator, scheduling intervals |
| Interaction Service | text/attachments gateway; where are files stored? → helpful for debugging |

**Note on file storage**: Where are files going/stored? → would be helpful for debugging issue
- If task moves to different state → event triggered

---

## 2026-04-20 (Monday) — Dashboard Presentation Day

### Presentation Summary
- **Presented CloudWatch Dashboard** to team/stakeholders
- Priority #1: export as **CSV / ABS files**
- Priority #2: **Centralized dashboard** for all resources
- Currently supported compute targets: **Lambda, EC2 (West), ECS, EKS**
- Next: **EC2, ECS, EKS** (West region) to be added

### Feedback Received

| Issue | Detail |
|-------|--------|
| OOM + Timeout | Need better logging for Out of Memory and Timeout errors |
| Current logging | Falls back to error-only (error/info/warnings → error fallback) |
| OOM log format | Log exception to stdout: `Out of Memory: "Message"` |
| Timeout log format | Log timeout errors with standard format: `Time out Error: "Message..."` |
| Dynamic filters | Apply filters dynamically — tool suggested: **Grafana** |
| Export granularity | **1 day** (every day) — currently defaults to **168h** (7-day period?) |
| Export API | Single API endpoint that exports data by **selecting a date range** |

### Grafana — Dynamic Filtering
*(From notebook S4 — Apr 20)*
- Team suggested using **Grafana** as the visualization layer for applying filters dynamically
- Grafana enables: per-project, per-client, per-date filters on dashboard panels
- Currently the dashboard does not support dynamic filter application — this is the gap

### Dashboard — Events View (Phase 1 vs Phase 2)
*(From notebook S6, S7 — Apr 20 / training week)*

**What's currently accessible (Phase 1):**
- Events view: current access only
- Filter by: **project, client, dates**
- Show: **progress of event**

**Phase 2 additions (planned):**
- `# of events received` per day
- `# of actions taken` per day → **Monitor + Alerts**
- `# of clients processed` — with **min / max / time** stats per process
- `process / clients` breakdown view
- ⚠️ **ODL access is currently given for only one tenant** — multi-tenant ODL access is a blocker for full dashboard
- ⚠️ **If there are no events for a process for a month** → flag as anomaly (monitoring gap)

---

## 2026-04-14 (Late Notes) — Config Service + Apr 15 MESO Config

### Config Service (CitcoWorks)
*(From notebook Screenshots S1, S2, S3 — Apr 14)*

**Core purpose:**
- ★ **Config Service = Host of all config** (centralized)
- Every app and environment has its own config hosted here
- Can be used for **SLAs** (explicitly noted)
- Drives automation build: `Config UI → Build Automation from Config`
  - The Config UI is the entry point; automation is constructed from what's defined in config

**Config metadata schema (RPA/IA context):**

| Field | Values / Notes |
|-------|---------------|
| `Type` | System / Env-specific (free text) |
| `Area` | ACTION-PROCESSOR, SERVICES, MESO |
| `Subject` | SOCL-CHECKS, TASK-MGMT, RPA (BU-specific) |
| `Tenant ID` | BU-specific identifier |
| `Label` | Description / free text |
| `Data.Sections` | list of dicts ✓ |
| `Data.Config` | config payload block |

**Config EPIC** = Centralized Config for IA initiative (cross-team)

### 2026-04-15 (Wednesday) — MESO Config + AP Architecture

**MESO Config on SharePoint:**
- `Client`, `Fund Group`, `Fund` (list of funds), other fields
- `Database name`, UIPath: taken daily/weekly — building for **monthly**, all processes not just quarterly

### Action Processor (AP) — Bhanu Teja Murari deep dive:
```
Event → Event Service (E.S.)
             ↓
Event Routing Service (E.R.S.)
             ↓ Kafka listener
  → injested → save or forward to topic subscriber (downstream sys)
  → check DB for route config
  → frame action message
  → call Kafka topic subscriber
             ↓
AP → performs business logic
   (task updates, task register, handle_socl_check)
```
- **ERS-AP Registry**: built via **deployment_specs.yaml** (`pytoml` library) — confirmed Apr 15
- **Who is responsible for transferring events to AP?** → ERS Kafka listener; AP process listens to Kafka topic subscriber
- KG instances: saved in **ODL** + **DynamoDB** (tasks, Cypher queries, hourly/quarterly/monthly)
- **Scheduler**: defined via deployment config; running hourly; objects saved in DynamoDB
- **Who is responsible for running the event scheduler?** → Bhanu Teja Murari's team (action required — confirmed)

### Task Interaction Service
- Gateway for task interactions
- Capability for the last task in the chain (chair)
- For each interaction task there is one event

### AXI / Axeo — Investment A/C Tool
- **AXI / Axeo** = Investment Account Management tool
- **Consumes from Meridian Notification Service** → applies rules
- **Notification Service capabilities** (confirmed Apr 16 with Bhanu Teja Murari):
  - ✅ Can send **email**
  - ✅ Push messages to **SQS**
  - ✅ Push messages to **SNS**

---

## 2026-04-16 (Thursday) — RPA Support Walk-Through (Horace)

### VPL (Valuation Price Listing) — Daily Process
- **VPL**: Daily at **5 PM EST** (NYSE closing price)
- Check which bots have jobs → **~4.5 jobs** typical queue
- **Event Store**: used for logging VPL runs

### Corp Actions — Voluntary (VCA)

**BPShare — Voluntary Corporate Actions:**
- **Input**: IP/filename + process filename → check filename first → if not found → ask to resend
- Processed → with summary → ingested file

**Queue Management (VCA):**
- `Paydown process`, **AVCAB / AV CAB** → VCA client list
- **Two Sigma** ops: BPShare → Two Sigma ops transfer
  - Ask them: **Strategy 0 → Non-Streety**
  - If moves from BPShare ops transfer → (BP there Two Sigma ops trigger)
  - Client will **send file to chat**

### Corp Actions — Mandatory (PROD, Saturdays)

**PROD — Running in Blue Prism:**
- Mandatory Corp Actions: `s/r 1 process`
- Error log → GTL → Intermediate employee
- UPS Transfer → Trigger Blue Prism
- **AVCAR Project** → session management
- **Voluntary QPS**: trends log → Voluntary Ops Transfer / Mandatory Ops Transfer
- Set `ourbl_fund` today

**E-Binder Management:**
- OTCK, **NewPool1** (afters), **NOWPOOL2**
- No e-binder because of time zone
- **VCA Opstruns**: sometimes doesn't pick up → must be today
- Processed ones have Summary → don't use → ask for new file

**E-Binder Queue Status:**
- Session might consolidate
- Pending requests in queue → ~20 pending
- `el-moder` → Query → Connect → web service → `ebinder_check`
- From d-vil → add bots → Monthly job + start
- If recon → then recon → e-binder initiate → check → run VIL bot → Meritory

### RPA Support — General (Citco IT, Case by Case)
- Hand Over: Confluence Page, Queue status
- UIPath Cloud: Exception Support Page
- Portfolio Data of DYMON-ASIA
- **FX Closeout**: `fx 10300/-` → pays → start parameter
- `Skip_Comploading` → act → DYMON-Arit → skip → `sump_True`

**UIPath (Cloud) vs Non-Cloud:**
- **SSO** for cloud UIPath processes
- **Data Ops** → `Fx-settled` → Status
- Argument: `In client` → `to PL-ReRun` (Daily VPL Run)
- If placed → Blue Prism / UIPath Bigger FILE
- **Gov/Kay sub-process here**
- Request → aligns with Ledger
- Need to **re-drop files for users** (they cannot do it themselves)

---

## RPA Support — CAIS Pricing Pack (Sridhar Ticket)

- **CAIS Pricing Pack** — Intelligent Automation support ticket
- Auth name/details + logs → location: **UCM**
- Query: `SELECT * FROM history.CAISRUM`
- **CAIS WB-PROD** → Search Query → client name, process log
- Save to local machine → check log → errors
- Copy Zip to local → unzip → **Closeout**
- `UIPath Prod 1 DataOps fxsettle_soln` → drop to UCM drive
- `Not choext_1` given → UIPath Prod → remove
- **BPShare location**: `\\bbhagat` (our pwd, Citcoext)

**Both CA types:** Voluntary + Mandatory (Horace)

---

## MESO Support — Aexeo Reporting DB Query

```
Path: ia/shared/warter/shared_meso_worker
Query by: client, fund, run_id
```
- **MESO Query** → `ia-16/shared/warter/shared_meso_worker/lyre`
- Fields: `client`, `fund`, `run_id`

---

---

## Workshop Open Questions & Action Items (`workshop_2026-04_training_week.md`)

These are unresolved questions logged during the training week requiring follow-up with **Bhanu Teja Murari** (BMurari@citco.com) / platform teams. Items already answered elsewhere in this document are excluded.

### Day 2 (2026-04-14) — Context: ADIC / KG / Event Services

| # | Question | Status |
|---|---|---|
| — | Where is the optic loader hosted? | Open |
| — | What is `GraphName`? (Neptune DB API) | Open |
| — | How would we know that data has been synced? | Open |
| — | Can we get the CyberQuery? | Open |
| — | Can we get access to the Knowledge Graph to see what has been updated? | Open |
| — | What is workflow API service? (used to get data) | Open |
| — | Who is responsible for adding configuration — platform team or business team? | Open |
| — | How does a business item propagate to the Knowledge Graph? | Open |
| — | Who decides the following: Process/Sub-Process, Task/Sub-Task hierarchy? | Answered: KG Loader |
| — | Who decides the dependencies (relations) on the Knowledge Graph? | Answered: KG Loader |
| — | How are the relations on the Knowledge Graph added? | Answered: via KG loader sync from Optics |
| — | Clarify levels L1–L5: L1=Product (certain), L2–L5 unclear | Open |
| — | Can the routing service perform a pass-through? | Answered: NO |
| — | If source-field data is missing, will blank data be sent? | Answered: YES |
| — | If `advanced_matching` data is missing, will it fail? | Answered: YES |
| — | Who is responsible for transferring received events to the Action Processor? | Open |
| — | How does a message reach the AP? | Open |
| 23 | What is the architecture behind the Event Routing Service? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 29 | Who is the AuthMaster team? (Bhanu knows a person in Hyderabad) | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 31 | What is the TTL (Time-To-Live) for a cache? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 32 | How often is the cache flushed? (e.g., on service restart) | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 40 | What is the process for time-card generation? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 44 | How would we know that data has been synced? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 46 | Can we get access to the Knowledge Graph to see what has been updated? | **@BMurari (Bhanu Teja Murari) – Action Required** |

### Day 3 (2026-04-15) — Context: ERS-AP

| # | Question | Status |
|---|---|---|
| — | How is the ERS-AP registry being built? | Answered: via `deployment_specs.yaml` (Pytoml library) |
| — | Where is the event scheduler defined? | Open |
| — | Who is responsible for running the event scheduler? | Answered: Bhanu Teja Murari's team |
| 47 | Can we get a Confluence page for how to manage (create, deploy) AP via ERS? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 48 | How to install VS Code plugin for deploying the AP? Where is it available / which marketplace? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 49 | URL of the place where AP is being saved? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 50 | Which CloudWatch groups, queries, logs to investigate for Action Processors? (Confluence Page) | **@BMurari (Bhanu Teja Murari) – Action Required** |

### Day 4 (2026-04-16) — Context: Notification Service / User Onboarding

| # | Question | Status |
|---|---|---|
| — | Can NS send an email? | Answered: YES |
| — | Can NS push messages to SQS? | Answered: YES |
| — | Can NS push messages to SNS? | Answered: YES |
| 51 | What is the process of onboarding a persona in CitcoWorks? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 52 | How should we investigate (check sync) if persona exists in both CitcoWorks and Optics? | **@BMurari (Bhanu Teja Murari) – Action Required** |
| 53 | Who defines the catalogue for the Reference Service? | **@BMurari (Bhanu Teja Murari) – Action Required** |

*End of Training Week Notes (April 13–20, 2026)*
*CTM Halifax — Bhavik Bhagat, Soundarya Venkataraman, Sridhar Vadla*
*Mentors: Horace, Kishor, JD, Bhanu Teja Murari (BMurari@citco.com — Innovation IT, HYD1)*

---

## Acronym Reference

| Acronym | Meaning |
|---------|---------|
| ADIC | Application Development & Integration Center |
| AP | Action Processor |
| CDI | Citco Document Intelligence |
| ERS | Event Routing Service |
| ERS-AP | Event Routing Service – Action Processor |
| HIL | Human-In-the-Loop |
| KG | Knowledge Graph |
| L1–L5 | Knowledge Graph hierarchy levels (Product → Sub-task) |
| LOB | Line of Business |
| NS | Notification Service |
| ODL | Operational Data Lake |
| TTL | Time-To-Live (cache) |
| AuthMaster | Authentication & Authorization Service |
| VPL | Valuation Price Listing |
| VCA | Voluntary Corporate Actions |
| MESO | Middle Office Month-End Sign-Off |
| AXI | Aexeo Investment Account Management tool |
| COMAPI | COM-based API (technology-dependent, used by Aexeo) |
| UCM | Unified Content Manager (file share) |
| SSO | Single Sign-On |
