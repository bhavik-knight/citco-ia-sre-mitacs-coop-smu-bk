# 2026-05-15 FRI

**Meetings:** W9 — Daily Standup (1:00–1:30 PM) · [Demo] RPA Support Agent (9:00–9:30 AM)
**Dev Hours:** 8.0h
**Meeting Hours:** 0.81h
---

**Work Log — IISS-487: Lambda Panels, $platform Variable & SQS SQL Migration — ia-it-sre-infrastructure-monitoring-amg**

| Time | Commit Message |
|------|----------------|
| 06:24 | docs: overhaul developer-guide - remove manual AWS CLI steps [IISS-487] |
| 06:46 | fix: SQS panels - Metric Insights GROUP BY without LIMIT, DLQ timeseries, gauge threshold labels off [IISS-487] |
| 07:26 | fix: SQS row — Metric Insights SQL with GROUP BY, $queue variable, DLQ filter [IISS-487] |
| 09:42 | feat: add $function variable to all panels, update SQS Health gauge thresholds [IISS-487] |
| 10:21 | fix: Concurrent Executions — per-function breakdown, mirrors SQS format [IISS-487] |
| 10:41 | fix: Error Rate — Metric Insights SQL + math per-function error % [IISS-487] |
| 10:55 | feat: split Duration into p50 + p99 panels — per-function breakdown [IISS-487] |
| 12:07 | fix: Lambda row — all panels auto period, Throttles timeseries, noValue, Error Rate labels [IISS-487] |
| 13:00 | hotfix: gridPos fix rows 04-10, Lambda rename lda- prefix, move infra/lambdas to lambdas/ [IISS-487] |
| 15:56 | hotfix: remove ECS Container Insights Lambda, stack, and related files [IISS-487] |
| 18:45 | feat: Lambda variable filter, cfn-lint as library, version notes, execution role lda-meridian perms [IISS-487] |
| 19:16 | feat: split Lambda error panels, remove Error Rate, pair Throttles+Concurrency [IISS-487] |

- Overhauled the developer-guide by removing manual AWS CLI steps for a cleaner workflow.
- Fixed SQS panels with Metric Insights GROUP BY without LIMIT, DLQ timeseries, and turned off gauge threshold labels.
- Fixed SQS row with Metric Insights SQL using GROUP BY, `$queue` variable, and DLQ filter.
- Added `$function` variable to all Lambda panels and updated SQS Health gauge thresholds.
- Fixed Concurrent Executions panel with per-function breakdown mirroring the SQS format.
- Fixed Error Rate panel with Metric Insights SQL and math for per-function error percentage.
- Split Duration panel into p50 and p99 panels with per-function breakdown.
- Fixed Lambda row with all panels using auto period, Throttles as timeseries, noValue handling, and Error Rate labels.
- Applied gridPos fixes to rows 04-10, renamed Lambda with `lda-` prefix, and moved `infra/lambdas` to `lambdas/`.
- Removed ECS Container Insights Lambda, stack, and related files as a hotfix.
- Added Lambda variable filter, cfn-lint as library, version notes, and execution role `lda-meridian` permissions.
- Split Lambda error panels, removed Error Rate panel, and paired Throttles with Concurrency for better organization.

## Excel Entry

**Task:** Lambda Panels, $platform Variable & SQS SQL Migration
**Description:**
- Overhauled the developer-guide by removing manual AWS CLI steps for a cleaner workflow.
- Fixed SQS panels with Metric Insights GROUP BY without LIMIT, DLQ timeseries, and turned off gauge threshold labels.
- Fixed SQS row with Metric Insights SQL using GROUP BY, $queue variable, and DLQ filter.
- Added $function variable to all Lambda panels and updated SQS Health gauge thresholds.
- Fixed Concurrent Executions panel with per-function breakdown mirroring the SQS format.
- Fixed Error Rate panel with Metric Insights SQL and math for per-function error percentage.
- Split Duration panel into p50 and p99 panels with per-function breakdown.
- Fixed Lambda row with all panels using auto period, Throttles as timeseries, noValue handling, and Error Rate labels.
**Hours:** 8.0
**Type:** dev
