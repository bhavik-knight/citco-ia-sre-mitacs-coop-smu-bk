% ============================================================
% CHAPTER 4: PROJECT --- OVERVIEW, CONTEXT & OBJECTIVES
% MCDA 5585 / 5586 Major Project Report
% Bhavik Kantilal Bhagat | A00494758 | Saint Mary's University
% ============================================================

\chapter{Project Overview, Context \& Objectives}
\label{chap:project}

This chapter provides a full account of the engineering project I undertook
during this internship. It begins with the production incidents that established
the monitoring gap and motivated the work, describes the three project streams
and their goals, maps every deliverable to its scope, and closes with the
platform context, repositories, and the domain immersion that grounded the
technical decisions.

% ────────────────────────────────────────────────────────────
\section{Project Background and the Motivating Incident}
\label{sec:project_background}
% ────────────────────────────────────────────────────────────

\subsection{Pre-Internship Monitoring Posture}

Before my internship began in mid-Mar~2026, the IA-SRE team operated
without a centralized observability stack. Monitoring relied entirely on
an \emph{ad~hoc} model: business teams in Dublin, London, and Halifax
would notice anomalies in fund processing outputs and raise tickets. There
were no CloudWatch alarms, no dashboards aggregating health across the six
platforms, and no mechanism for the SRE team to receive proactive
notification of degrading conditions --- the team was blind to
\emph{saturation} and \emph{error rate} signals until they had already
caused downstream business impact.

\subsection{CAIS-Pricing Silent Outage (Jan--Feb 2026)}
\label{sec:cais_silent_outage}

Between \textbf{Jan~20 and Feb~17, 2026}, the CAIS-Pricing Extract GenAI
pipeline under the \texttt{rpacfs1} platform stopped delivering output for
\textbf{29 consecutive days} without a single alert firing. The root cause
was gradual exhaustion of the external CTI AI model API token, which
controls the async SQS callback that triggers the downstream Lambda
stages; without that callback, three downstream functions dropped to zero
invocations while two upstream functions kept running, making the pipeline
appear healthy. The outage was detected only when business users reported
missing pricing data --- 29~days after onset. The post-mortem (IISS-430)
identified the absence of throughput alarms, SQS queue depth monitoring,
and end-to-end output verification as the root causes of the failure going
unnoticed. Sub-task IISS-435 was assigned directly to me as the immediate
remediation and became the starting point for the full monitoring programme.

\subsection{The Meridian Incident (Apr~2026)}

On \textbf{Apr~17, 2026}, a surge of automated system events from Citco
Recs congested Meridian's OpenSearch consumption layer, causing Kafka
consumer lag to reach approximately \textbf{9~million messages}. Because
AXI polls Meridian for BPM completion events, the stalled consumer halted
daily statement processing for multiple fund clients --- including Winton
and Goldman Sachs --- for \textbf{7.5~hours}. The key timeline: BPM
submitted its first event at 04:39~AST; AXI polled nine times with no
result; the SRE team received no alert; the Dublin business team escalated
at 08:13~AST, three and a half hours after the first failure signal.
The post-mortem recorded explicitly: \textit{``No proactive
monitoring/alerting in place on the SRE side for Meridian event volume
anomalies.''} Action items from that post-mortem became the core
requirements for IISS-487.

\subsection{From Incident to Programme}

Both incidents shared the same root cause: no proactive monitoring. The
post-mortems established two specific gaps --- no Kafka consumer lag alarm,
and no centralized visibility across the IA-IT-SRE Infrastructure --- that
directly motivated the three work streams described in the following
section. Kishor elevated IISS-487 to \textbf{Critical} priority on
May~6, 2026, and my internship pivoted from exploratory dashboard research
to a full-scale observability platform build.

% ────────────────────────────────────────────────────────────
\section{Project Overview}
\label{sec:project_overview}
% ────────────────────────────────────────────────────────────

The two motivating incidents described above defined the engineering mandate:
build a system where a service degradation that went undetected for weeks or
hours would instead trigger an alert within seconds. Three parallel work streams
were organized to address this at different layers of the IA-IT-SRE
Infrastructure.

\subsection{Work Stream 1: SRE Observability Platform}

The primary deliverable was a centralized \textbf{Amazon Managed Grafana (AMG)}
dashboard providing single-pane visibility across all AWS services on all six
platforms. The workspace comprises 14 dashboard rows, each covering one AWS
service category --- Lambda, SQS, MSK/Kafka, EC2, ECR, EKS, ECS Services, ECS
Target Groups, RDS Aurora, DocumentDB, Redis/Valkey, Neptune DB, and OpenSearch.
Dynamic CloudWatch alarms are provisioned via a Lambda-backed CloudFormation
pipeline that discovers the current AWS service fleet at deploy time, eliminating
the need for manual alarm enumeration. Platform template variables allow
operators to filter the entire dashboard to a single platform or AWS service
with a single dropdown selection.

\subsection{Work Stream 2: IA Infrastructure Monitoring}

Preceding and parallel to the Grafana platform, this stream developed
\textbf{per-platform CloudWatch dashboards} for Lambda, ECS, EC2, SQS, and
related AWS services across all six platforms. It established the architectural
patterns --- Metric Insights SQL queries, tag-based resource discovery, the
\texttt{AppName} fallback strategy --- and the resource inventory that the
Grafana platform builds on. It also produced a registry-based SQS monitoring
pipeline with a three-tier fallback model for resilient queue data retrieval.

\subsection{Work Stream 3: IA Infrastructure Inventory API}

The third stream produced a \textbf{Flask REST API} for programmatic discovery
of all deployed AWS services across the IA-IT-SRE Infrastructure. The API uses
a discoverer registry pattern that maps \texttt{(resource\_type, platform)}
tuples to concrete discoverer classes, enabling the fleet inventory to stay
current as platforms grow without requiring changes to the API routing layer.
The API covers Lambda functions, ECS clusters, EC2 instances, SQS queues, and
S3 buckets across all six platforms.

\subsection{RPA Production Support}

A fourth, ongoing responsibility ran in parallel: four dedicated support
rotation weeks (11~AM -- 7~PM~EST, once every four weeks), during which the
primary focus shifted from development to ensuring the reliability of Blue Prism
and UiPath automation workloads in production. This included monitoring bot
queues, triaging service desk tickets from business teams across Citco, and
resolving or escalating production incidents.

% ────────────────────────────────────────────────────────────
\section{Project Goals}
\label{sec:project_goals}
% ────────────────────────────────────────────────────────────

The primary goals of the internship project were as follows:

\begin{itemize}

    \item \textbf{Eliminate reactive-only incident detection.} Replace the
          dependency on business-team escalation as the primary detection
          mechanism with proactive CloudWatch alarms that fire within seconds
          of a threshold being crossed, directly addressing the failure modes
          exposed by IISS-430 and the Apr~17 Meridian incident.

    \item \textbf{Build a centralized observability platform for the IA-IT-SRE
          Infrastructure.} Provide a single pane of glass across all six
          platforms and all 13 AWS service categories, enabling the SRE team
          to assess platform health, drill down to individual AWS services, and
          respond to incidents without navigating six separate CloudWatch consoles.

    \item \textbf{Automate AWS service discovery and inventory.} Replace manual
          Confluence-maintained Lambda lists with a programmatic, always-current
          inventory API that enumerates all deployed AWS services per platform
          and exposes them via REST endpoints for use by the dashboard and other
          downstream tooling.

    \item \textbf{Evaluate AWS-native observability as a Dynatrace alternative.}
          Conduct a structured feature-parity analysis comparing the AWS-native
          stack (CloudWatch, Grafana AMG, X-Ray, Application Signals) against
          Dynatrace capabilities, quantifying the achievable parity percentage
          and the incremental monthly cost at each tier.

    \item \textbf{Deliver Infrastructure-as-Code for all monitoring components.}
          Ensure all alarms, dashboards, IAM roles, and supporting infrastructure
          are provisioned via parameterized CloudFormation templates and AWS SAM,
          making the platform reproducible, version-controlled, and
          environment-portable.

    \item \textbf{Establish engineering quality standards.} Produce a test suite
          for the inventory API with 99\% code coverage, document all
          architectural decisions and limitations, and maintain a Confluence
          knowledge base for the Lambda inventory, runbooks, and incident
          post-mortems.

\end{itemize}

% ────────────────────────────────────────────────────────────
\section{Deliverables and Technical Scope}
\label{sec:deliverables_scope}
% ────────────────────────────────────────────────────────────

All engineering work during my internship was tracked through Citco's
JIRA instance under the \texttt{IISS} project key. Table~\ref{tab:jira_deliverables}
summarizes the key deliverables, their classification, and their
quantified impact.

\begin{table}[ht]
\centering
\caption{JIRA Deliverables --- Summary Table}
\label{tab:jira_deliverables}
\begin{tabular}{lp{5.5cm}p{5.0cm}}
\toprule
\textbf{JIRA} & \textbf{Title / Scope} & \textbf{Hours / Impact} \\
\midrule
\textbf{IISS-487} & Amazon Managed Grafana: Centralized SRE Dashboard & 140h logged; 14 rows, 110+ panels, 38 alarms, 7 platforms, IaC CFN provisioner \\
\midrule
\textbf{IISS-482} & IA Resources/Infrastructure Monitoring Platform & 11h logged; SQS registry, 3-tier fallback, 960 SQS tests \\
\midrule
\textbf{IISS-455} & Lambda Monitoring Dashboard (initial CloudWatch) & 2h logged; 51 Lambda functions, Confluence inventory \\
\midrule
\textbf{IISS-469} & Centralized Lambda Dashboard Improvements & Architecture analysis; Top-K limits documented, 5 OOM detection approaches \\
\midrule
\textbf{IISS-507} & IA Resources Inventory API (Flask) & 2.5h logged; 562 tests, 99\% coverage, discoverer registry \\
\midrule
\textbf{IISS-538} & MESO High-Memory/CPU Autoscale Issue & Hotfix; \texttt{ia\_meso\_process} ECS autoscaling configuration ($\sim$Jun~5) \\
\midrule
\textbf{IISS-706} & CAIS Deadline Hotfix --- UCM Credential Validation & 7h logged; SpnegoError root cause analysis; fail-fast credential validation; two-layer defence \\
\midrule
\textbf{IISS-741} & MESO Month-End Stuck Tasks (Critical) & Retrigger for INNOCAP/SAMLMOS; Jul~1, 2026 \\
\midrule
\textbf{IISS-753} & VPL IPV UI Selector Failure & 16h estimate; selector resilience investigation; Jul~8, 2026 \\
\midrule
\textbf{IISS-876} & VPL Pricing GTL Incorrect Booking & Root cause analysis and fix; Jul~30, 2026 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Centralized Grafana SRE Dashboard}

IISS-487 was elevated to \textbf{Critical} priority by Kishor Deotale on
May~6, 2026, in the weeks following the Apr~17 incident. It was the
largest single engineering effort of my internship, consuming 140~hours
over approximately six weeks of intensive development. Key milestones
within IISS-487 included:

\begin{itemize}
    \item \textbf{May~11, 2026}: Infrastructure deployment --- four
          CloudFormation stacks deployed into the IA SRE AWS account,
          including the Grafana AMG workspace, the SNS alarm topic,
          the S3 dashboard storage bucket, and the IAM roles for the
          16-role agent permission model required by Grafana's CloudWatch
          data source.

    \item \textbf{May~11--12, 2026}: Container Insights enablement ---
          five of the six Meridian ECS clusters were onboarded to
          Amazon CloudWatch Container Insights, providing CPU, memory,
          network I/O, and storage utilization telemetry at the task
          level. (One cluster, \texttt{meridian-compute-cluster}, had
          already had Container Insights enabled since Jul~2024.)

    \item \textbf{May~13, 2026}: Grafana version upgrade from
          \textbf{10.4 to 12.4} --- a major version migration requiring
          query syntax updates and panel configuration remediation across
          the entire dashboard.

    \item \textbf{May~21, 2026}: Secrets Manager 64KB limit breach ---
          the agentic CloudFormation provisioner grew to 14,720~lines,
          exceeding the parameter store limit; refactored to S3-backed
          template staging.

    \item \textbf{Jun~18, 2026}: Final deployment at version v728;
          total 140~hours reached; release merged to main branch.
\end{itemize}

\subsection{IA Infrastructure Monitoring Platform}

IISS-482 served as the parent story for the full IA monitoring platform.
Its sub-stories (IISS-483 through IISS-486) covered EC2, ECS, EKS, and
S3 export functionality. The SQS monitoring feature, added on
Jun~12, 2026, introduced a significant architectural contribution: the
\textbf{registry-based dashboard builder} with three-tier fallback,
which became the standard pattern for subsequent monitoring integrations.
The 960~tests written for this feature set the quality baseline for the
inventory API that followed.

\subsection{IA Infrastructure Inventory API}

IISS-507 (\texttt{ia-sre-resources-inventory}) was scaffolded on
Jun~11, 2026, as a new Flask async API project separate from the
existing monitoring repositories. My key architectural decision was the
\textbf{discoverer registry pattern}: rather than hardcoding resource
discovery logic per budget code, a registry maps
\texttt{(resource\_type, budget\_code)} tuples to concrete discoverer
classes. This design enables the team to onboard new budget codes or
resource types by implementing a single discoverer class and registering
it --- no changes to routing, serialization, or test infrastructure.

The tag-based discovery challenge was non-trivial. Two platforms --- \texttt{CITCOWORKS} and \texttt{MERIDIAN} --- share the
AWS tag value \texttt{``Shared Cloud Artifacts''} for the
\texttt{BudgetCode} dimension. A naive \texttt{BudgetCode} lookup would
therefore conflate resources from two distinct operational environments.
The solution was to designate the \texttt{AppName} tag as a mandatory
secondary discriminator for these two environments, with the discovery
logic performing a two-stage filter: \texttt{BudgetCode=``Shared Cloud
Artifacts''} followed by \texttt{AppName} $\in$ \{\texttt{citcoworks}, \texttt{meridian}\}.

\subsection{RPA Support Incidents and Hotfixes}

Four RPA support rotations (Weeks~8, 12, 16, and~20) and several
out-of-rotation incidents rounded out my project scope:

\begin{itemize}
    \item \textbf{IISS-706 (Jun~8, 7h)}: The CAIS Deadline process
          failed with a \texttt{SpnegoError} caused by empty UCM
          credentials. My root cause analysis revealed that the ECS
          container loaded credentials from Secrets Manager once at
          module import time via \texttt{constants.py}. When the UCM
          secret was rotated or expired between container restarts, the
          cached empty credential persisted for the entire container
          lifecycle. I implemented a fail-fast validation layer
          and a second credential check before the UCM upload step,
          making the failure visible immediately rather than manifesting
          as a cryptic downstream NTLM authentication error.

    \item \textbf{IISS-741 (Jul~1, Critical)}: MESO month-end tasks
          became stuck for the INNOCAP and SAMLMOS funds, requiring
          manual retrigger of the \texttt{ia\_meso\_process} pipeline.

    \item \textbf{IISS-753 (Jul~8)}: VPL IPV process encountered a UI
          selector failure, with a 16-hour resolution estimate involving
          selector resilience analysis across the UiPath automation.

    \item \textbf{IISS-876 (Jul~30)}: VPL Pricing GTL incorrect booking
          required root cause analysis and a targeted process fix.
\end{itemize}

% ────────────────────────────────────────────────────────────
\section{Acceptance Criteria and Success Metrics}
\label{sec:acceptance_criteria}
% ────────────────────────────────────────────────────────────

Success across the three work streams was defined in terms of both
\emph{functional} acceptance criteria (did the deliverable do what was
specified?) and \emph{quantitative} metrics (can the improvement be
measured?). The following subsections enumerate the criteria and the
evidence gathered against each.

\subsection{SRE Observability Platform}

\begin{itemize}
    \item \textbf{Consumer lag detection latency}: Improved from a
          manual discovery window of 2--3~minutes (business team
          escalation required) to \textbf{under 30~seconds} via
          automated CloudWatch alarm on the MSK
          \texttt{EstimatedMaxTimeLag} metric. This directly addresses
          the core failure mode of the Apr~17 incident.

    \item \textbf{Dashboard coverage}: 14~dashboard rows and
          110+~panels covering 7~AWS AWS service categories
          (\texttt{Lambda}, \texttt{ECS}, \texttt{MSK},
          \texttt{SQS}, \texttt{DocumentDB}/\texttt{Neptune},
          \texttt{OpenSearch}, \texttt{EC2}).

    \item \textbf{Alarm coverage}: 38~CloudWatch alarms deployed across
          all monitored platforms, each connected to SNS notifications
          routed to the SRE team's distribution list.

    \item \textbf{Container Insights}: Enabled on 5~of~6 Meridian ECS
          clusters (May~11--12, 2026), providing task-level CPU and
          memory telemetry that was absent prior to the internship.

    \item \textbf{Infrastructure deployment}: 4~CloudFormation stacks
          deployed on May~11, 2026, establishing the Grafana workspace,
          alarm infrastructure, and S3 storage as reproducible IaC.

    \item \textbf{Grafana version}: Workspace upgraded from 10.4 to
          \textbf{12.4} during development, ensuring the platform runs
          on a supported, feature-current release for the remainder of
          its operational life.
\end{itemize}

\subsection{IA Infrastructure Inventory API}

\begin{itemize}
    \item \textbf{Resource coverage}: Lambda functions
          discovered across 5~platforms; \textbf{22~ECS clusters}
          across 5~platforms; \textbf{83~EC2 instances} scanned
          with tag-based discovery.

    \item \textbf{Test coverage}: \textbf{562~automated tests}
          achieving \textbf{99\%~code coverage}. The test suite
          includes unit tests for each discoverer, integration tests
          for all REST endpoints, and property-based tests for the
          tag-matching and registry dispatch logic.

    \item \textbf{Tag disambiguation}: The
          \texttt{AppName}-based secondary filter correctly separates
          \texttt{CITCOWORKS} from \texttt{MERIDIAN} resources sharing
          the \texttt{``Shared Cloud Artifacts''} BudgetCode tag,
          preventing inventory contamination between the two environments.

    \item \textbf{Extensibility}: The discoverer registry pattern allows
          new platforms or resource types to be onboarded by
          implementing a single class implementing the
          \texttt{BaseDiscoverer} interface, with zero changes required
          to the API routing or serialization layers.
\end{itemize}

\subsection{RPA Support Operations}

\begin{itemize}
    \item \textbf{CAIS Deadline hotfix (IISS-706)}: A fail-fast
          credential validation layer was delivered within 7~hours of
          the initial incident report, preventing recurrence of the
          empty-credential container lifecycle problem. The hotfix
          was reviewed and merged to a release branch for production
          deployment.

    \item \textbf{Support rotation coverage}: 4~production support
          rotations completed (Weeks~8, 12, 16, 20) without SLA
          breach, covering VPL queue monitoring (5~PM~EST daily),
          Corporate Actions triage, MESO month-end operations, and
          BPM/UiPath platform incidents.
\end{itemize}

\subsection{Summary of Quantitative Outcomes}

\begin{table}[ht]
\centering
\caption{Key Quantitative Outcomes}
\label{tab:quant_outcomes}
\begin{tabular}{lll}
\toprule
\textbf{Metric} & \textbf{Pre-Internship} & \textbf{Post-Internship} \\
\midrule
MSK consumer lag detection time & 2--3~min (manual) & \textless30~sec (alarm) \\
CloudWatch alarms on Meridian & 0 & 38 \\
Dashboard panels (centralized) & 0 & 110+ \\
Container Insights clusters (Meridian) & 1~of~6 & 5~of~6 \\
Lambda functions inventoried & Manual/partial & Automated (all platforms) \\
ECS clusters inventoried & Manual/partial & 22 (automated) \\
EC2 instances scanned & Manual/partial & 83 (tag-based) \\
Inventory API test coverage & N/A & 562 tests, 99\% \\
CFN stacks (IaC) & 0 & 4 deployed \\
\bottomrule
\end{tabular}
\end{table}

Taken together, my three work streams transformed the IA-SRE team's
monitoring posture from a reactive, business-team-dependent model to a
proactive observability platform with sub-minute alerting, comprehensive
resource inventory, and infrastructure-as-code governance. The Apr~17
incident served as the catalyst; the deliverables described in the
subsequent chapters constitute my engineering response.


% ────────────────────────────────────────────────────────────
% NOTE: Team composition and mandate are documented in Chapter 1
% (About Company & Organization, Section 2.2 Team Structure).
% ────────────────────────────────────────────────────────────

% ────────────────────────────────────────────────────────────
\section{The Meridian Platform}
\label{sec:meridian_platform}
% ────────────────────────────────────────────────────────────

\textbf{Meridian} is Citco's core digital workflow platform for fund
operations. It handles event processing, task management, and document
workflows for fund administration --- serving as the operational backbone
through which business events (fund completions, report deliveries, task
assignments) flow between Citco's systems and external counterparties.
Understanding Meridian's architecture was prerequisite to my designing
meaningful observability coverage for it.

\subsection{Meridian Components Relevant to Observability}

The following Meridian components were my primary observability targets
during the internship:

\begin{itemize}
    \item \textbf{Event Routing Service (ERS).} Routes events between
          services using a mapping table that specifies area, subject,
          source, type, and destination Action Processor. Events are stored
          in an ODL/Redis Elasticache layer and then pushed to Kafka topics
          for consumption. ERS cannot perform a pass-through without a
          mapping entry; missing \texttt{advanced\_matching} fields cause
          routing failures.

    \item \textbf{Action Processor (AP).} Consumes routed events from Kafka
          and executes the corresponding business logic (e.g., updating task
          status, triggering downstream notifications). APs are registered
          via \texttt{deployment\_specs.yaml} using the PyTOML library and
          can be anything from a simple status update to a cross-system
          integration call.

    \item \textbf{OpenSearch Consumer.} Consumes events from Kafka topics
          and indexes them into the OpenSearch search cluster, making events
          queryable by downstream services such as AXI. This is the
          component whose throughput was overwhelmed on Apr~17, 2026 ---
          the incident that directly motivated the SRE observability work.

    \item \textbf{Apache Kafka / Amazon MSK.} The message backbone of the
          Meridian event architecture. Kafka topics serve as the durable
          buffer between event producers (ERS) and consumers (OpenSearch
          consumer, APs). Consumer lag on these topics is the primary
          leading indicator of event processing delays.

    \item \textbf{ECS Clusters (Meridian).} Six ECS clusters host the
          containerized Meridian services:
          \texttt{meridian-api-cluster},
          \texttt{meridian-cluster-compute},
          \texttt{meridian-cluster-services},
          \texttt{meridian-services-cluster},
          \texttt{meridian-ui-cluster}, and
          \texttt{meridian-compute-cluster}.
          I enabled Container Insights on five of these six clusters on
          May~11--12, 2026 (IISS-487), providing CPU, memory, network I/O,
          and storage utilization telemetry at the task level.
\end{itemize}

\subsection{The Apr 17, 2026 Incident}

On \textbf{Apr~17, 2026}, event processing on the Meridian platform
experienced a 7.5-hour delay that prevented AXI from receiving BPM
completion events for daily statement processing. The root cause was a
high volume of automated system events from Citco Recs, which overwhelmed
the OpenSearch consumption layer. Events were received successfully in
Databricks but were not being consumed into OpenSearch --- backing up
approximately \textbf{9~million messages} on the Kafka topics. Multiple
funds were affected, including Winton, Goldman Sachs dailies, and others.

The timeline illustrates the core observability gap: BPM submitted the
first completion event at 04:39 AST; AXI polled nine times over the
following two hours with no response; and the issue was not flagged until
the \textbf{Dublin business team raised it at 08:13} --- over three and a
half hours after the first failure signal.

\begin{table}[ht]
\centering
\caption{Apr 17, 2026 Meridian Incident --- Key Timeline (AST)}
\label{tab:apr17_timeline}
\begin{tabular}{lp{10cm}}
\toprule
\textbf{Time (AST)} & \textbf{Event} \\
\midrule
04:39 & BPM submits Deliver2 completion event for \texttt{ap\_id} 706648 \\
04:53--06:41 & AXI polls Meridian 9 times --- event not received \\
07:16 & BPM submits event for \texttt{ap\_id} 712661 \\
08:13 & Dublin business team flags the issue \\
09:30 & Root cause identified by Meridian team \\
09:56 & Communication sent --- ETA 2--3 hours \\
$\sim$12:00 & System restored to normal processing \\
\bottomrule
\end{tabular}
\end{table}

The resolution involved doubling the concurrency and Kafka partition counts
for the OpenSearch consumer (30$\to$60 concurrent consumers, 30$\to$60
partitions), upgrading the OpenSearch cluster from a \texttt{t3.medium} to
\texttt{m4.large} with the node count increased from 3 to 5, and upscaling
the Interactions Service and Tasks Management API memory allocations.

The incident directly exposed a critical gap in the team's monitoring
posture: \textbf{no proactive monitoring or alerting was in place on the
SRE side for Meridian event volume anomalies.} The SRE team had no
visibility into Kafka consumer lag, OpenSearch ingestion rates, or ECS
task-level resource saturation. This gap became the primary motivation for:

\begin{itemize}
    \item \textbf{IISS-487} --- the centralized Grafana SRE dashboard,
          designed to surface Meridian consumer lag as a leading indicator;
    \item \textbf{IISS-489} --- the formal SRE Observability Roadmap, which
          mapped monitoring gaps to prioritized engineering actions;
    \item \textbf{IISS-488} --- the Auto-Healing Pipeline initiative, aimed
          at reducing the mean time to recovery for recurring failure modes;
    \item \textbf{IISS-506} --- the Meridian monitoring infrastructure
          project, providing a full gap analysis and remediation plan.
\end{itemize}

\subsection{The Earlier Observability Blind Spot (January--Feb 2026)}

The Apr~17 incident, while the most dramatic and well-documented failure,
was not the first signal of an observability gap at Citco. In the
\textbf{five-week period from mid-Jan to mid-Feb 2026}, a
production service was degraded or effectively down without any member of
the development, business, infrastructure, or DevOps teams realizing it.
The failure was eventually surfaced --- not by any automated alert or
monitoring system --- but by a client complaint that arrived weeks after the
underlying problem had begun affecting fund operations.

This five-week blind spot pre-dated the formal SRE engagement by several
months and had no documented root cause analysis because the problem had
persisted silently for so long that the precise onset was not recoverable
from logs. The incident became the organizational memory that gave weight to
Kishor's Dynatrace replacement initiative: it was not a hypothetical
reliability risk but a demonstrated pattern of extended, undetected service
degradation. The Apr~17 incident confirmed the pattern was still present
after the IA platform had grown to its current scale.

Together, these two incidents define the \textbf{observability mandate} that
governed my internship: build a system where a service degradation that
would have been invisible for five weeks in Jan~2026 would instead
trigger an alert within 30 seconds. The MSK consumer lag alarm I implemented --- which
would have fired within minutes of the Apr~17 backlog beginning to build
--- is the clearest demonstration that this mandate was met.

% ────────────────────────────────────────────────────────────
\section{The Intelligent Automation Platform}
\label{sec:ia_platform}
% ────────────────────────────────────────────────────────────

Meridian is one component of a broader \textbf{Intelligent Automation (IA)
platform} operated by the IA-SRE team. The platform is organized into five
\emph{platforms}, each hosting a distinct class of
automation workload. Understanding this structure was essential for me to
design a resource inventory system that could enumerate all monitored
assets without manual cataloguing.

\subsection{Budget Code Environments}

\begin{table}[ht]
\centering
\caption{IA Platform --- Budget Code Environments and Resource Counts}
\label{tab:budget_codes}
\begin{tabular}{llcc}
\toprule
\textbf{Budget Code} & \textbf{Platform} & \textbf{Lambda} & \textbf{ECS Clusters} \\
\midrule
\texttt{rpacfs1}      & CFS RPA Automation          & $\sim$30 & 5 \\
\texttt{ia-bpo}       & BPO Intelligent Processing  & $\sim$30 & 6 \\
\texttt{automationhub}& UiPath Automation Hub       & 22       & 1 (+1 EKS) \\
\texttt{meridian}     & Meridian Workflow Platform  & 32       & 6 \\
\texttt{citcoworks}   & CitcoWorks Workflow Engine  & 5        & 5 \\
\midrule
\textbf{Total} & & \textbf{$\sim$119} & \textbf{22 ECS + 1 EKS} \\
\bottomrule
\end{tabular}
\end{table}

In addition to serverless and containerized workloads, the platform
includes 8~EC2 instances (a mix of running and stopped) distributed across
the five environments. Tag-based resource discovery --- using the
\texttt{BudgetCode} tag as primary key and \texttt{AppName} as a fallback
--- was required for the inventory API (IISS-507) because
\texttt{CITCOWORKS} and \texttt{MERIDIAN} share a ``Shared Cloud
Artifacts'' BudgetCode that is not unique at the environment level.

\subsection{RPA Platforms and Key Business Processes}

The IA platform's automation workloads operate on two RPA technologies:

\begin{itemize}
    \item \textbf{Blue Prism} handles daily, monthly, and quarterly
          fund operations automation. Blue Prism is deployed in pools of
          licensed robot nodes; each pool handles a designated class of
          work (e.g., DataOps P1--P20 in PROD).

    \item \textbf{UiPath} --- both UiPath Cloud (most processes, post
          migration) and UiPath on-premises (non-cloud, for processes such
          as FX Settled Status audits that run within the Citco VPC) ---
          is the target platform for Blue Prism migrations and hosts newer
          automation processes.
\end{itemize}

The key business processes monitored by the IA-SRE team include:

\begin{itemize}
    \item \textbf{VPL Pricing} --- Valuation and Pricing (VPL) queue
          monitored from 5~PM EST daily. Three primary bots (with capacity
          for three additional) handle fund NAV and pricing completion
          events under a tight business deadline driven by market close.

    \item \textbf{Corporate Actions} --- Mandatory, Voluntary, and
          Operations Transfer workflows, plus Two Sigma wire transfers.

    \item \textbf{MESO} --- Middle Office Month-End Sign-Off
          (\texttt{ia\_meso\_process}), a Python and AWS-based process
          handling cash and position reconciliation, Aexeo report
          generation, and E-Binder packaging for fund administration teams.

    \item \textbf{CAIS Deadline} --- Consolidated Audit Trail (CAIS)
          submission automation under the \texttt{ia-bpo} budget code,
          subject to regulatory filing deadlines.
\end{itemize}

% ────────────────────────────────────────────────────────────
\section{Development Repositories and Project Scope}
\label{sec:repos_scope}
% ────────────────────────────────────────────────────────────

The engineering work during my internship spanned \textbf{four primary
CodeCommit repositories} and a broader set of supporting repositories.
Understanding this multi-repo scope is important context for interpreting
the JIRA hierarchy, because many deliverables I tracked under a
single JIRA (e.g., IISS-487) involved work across multiple repositories, and
some earlier JIRA work was superseded or fulfilled by later, larger
deliverables.

\subsection{Primary Repositories}

\begin{itemize}
    \item \textbf{\texttt{ia-it-sre-infrastructure-monitoring-amg}} --- my primary
          delivery repository for the Grafana AMG observability platform (IISS-487).
          Contains the CloudFormation IaC templates, Lambda provisioner, dashboard
          panel YAML files (per-row, per-panel structure), deployment scripts, and
          the full test suite (960+ tests). This repository is the central artifact
          of my internship.

    \item \textbf{\texttt{ia-resources-monitoring}} --- the earlier CloudWatch
          dashboard platform (IISS-455, IISS-469, IISS-482--486), developed in
          parallel to the Grafana work. Contains the Python-based CloudWatch
          dashboard builders for Lambda, EC2, ECS, EKS, and SQS monitoring, the
          registry API that powers tag-based resource discovery, and the S3 export
          pipeline. The architectural patterns established in this repository ---
          particularly the registry pattern and 3-tier fallback model --- directly
          informed the design of the Inventory API.

    \item \textbf{\texttt{ia-it-sre-infrastructure-inventory} (ia-sre-resources-inventory)} ---
          the Flask REST API for programmatic cloud asset discovery (IISS-507, IISS-508).
          Contains the discoverer registry, budget-code-specific discoverers, Pydantic
          response models, and the 562-test automated suite (99\% coverage). This
          repository provides the machine-queryable resource inventory that the
          Grafana dashboard's Row~13 resource panel links to.

    \item \textbf{\texttt{rpacfs1-cais-pricing-extract-genai-infrastructure}} --- the
          RPACFS1 automation infrastructure repository, where the initial CloudWatch
          dashboard and Lambda monitoring work (IISS-435, IISS-455 early commits)
          was prototyped before migrating to the dedicated monitoring repository.
\end{itemize}

\subsection{Full Project Scope and Tracking}

Table~\ref{tab:full_jira_scope} provides the complete JIRA inventory for the
internship. A key observation is that the CloudWatch dashboard work (IISS-455,
IISS-469, IISS-482--486) and the Grafana platform (IISS-487) are
\emph{complementary rather than redundant}: the CloudWatch dashboards provide
native AWS Console visibility per resource type, while Grafana AMG provides
the unified cross-service operational view. Work done under IISS-482 through
IISS-486 also contributed directly to IISS-487's scope --- particularly the
Container Insights cost analysis (IISS-484), the EC2 tag strategy (IISS-483),
and the EKS cluster scope definition (IISS-485).

\begin{table}[ht]
\centering
\caption{Full JIRA Scope --- Internship Deliverables}
\label{tab:full_jira_scope}
\begin{tabular}{p{1.8cm}p{5.5cm}p{4.5cm}}
\toprule
\textbf{JIRA} & \textbf{Title / Scope} & \textbf{Status / Repo} \\
\midrule
IISS-435  & RPACFS1 CloudWatch pipeline health dashboard & Done / rpacfs1-cais \\
IISS-455  & Lambda monitoring CloudWatch dashboard (51→119 functions) & Done / ia-resources-monitoring \\
IISS-461  & Lambda inventory Confluence page (51 functions) & Done / Confluence \\
IISS-469  & Improved centralized Lambda dashboard; Top-K analysis & Done / ia-resources-monitoring \\
IISS-482  & IA Resources/Infrastructure Monitoring Platform (parent) & In Progress / ia-resources-monitoring \\
IISS-483  & EC2 monitoring dashboard; tag mismatch discovery & In Progress / ia-resources-monitoring \\
IISS-484  & ECS monitoring dashboard; Container Insights cost analysis & In Progress / ia-resources-monitoring \\
IISS-485  & EKS monitoring dashboard; AUTOMATIONHUB scope & On Hold / ia-resources-monitoring \\
IISS-486  & Export to S3 (scheduled Lambda, Excel export) & In Progress / ia-resources-monitoring \\
IISS-487  & Amazon Managed Grafana SRE Dashboard (primary deliverable) & 140h / ia-it-sre-infrastructure-monitoring-amg \\
IISS-488  & Auto-Healing Pipeline (SNS → Lambda remediation) & To Do / roadmap \\
IISS-489  & Meridian SRE Observability Roadmap & To Do / roadmap \\
IISS-504  & Extend registry API for AUTOMATIONHUB budget code & In Progress / ia-resources-monitoring \\
IISS-505  & Extend registry API for MERIDIAN & In Progress / ia-resources-monitoring \\
IISS-506  & MERIDIAN Monitoring Infrastructure (gap analysis) & To Do / roadmap \\
IISS-507  & IA Resources Inventory API (Flask async REST API) & In Progress / ia-it-sre-infrastructure-inventory \\
IISS-508  & Backend --- IA Resources (discoverer registry) & In Progress / ia-it-sre-infrastructure-inventory \\
IISS-509  & Frontend --- IA Resources (dashboard widgets) & To Do / ia-it-sre-infrastructure-inventory \\
IISS-510  & Extend registry API for CITCOWORKS & In Progress / ia-resources-monitoring \\
IISS-824  & ML/LLM RPA Status Agent documentation and improvements & In Progress / ml-rpa-status-llm-agent \\
IISS-837--844 & X-Ray distributed tracing (7 service sub-tasks) & To Do / ia\_meso\_process \\
IISS-845--873 & Log group discovery \& observability (10 sub-tasks) & To Do / ia-it-sre-infrastructure-monitoring-amg \\
IISS-538  & MESO high-memory autoscale issue (hotfix) & Done \\
IISS-706  & CAIS Deadline UCM credential validation hotfix & Done / core-ia-bpo-cais-dl \\
IISS-741  & MESO month-end stuck tasks (Critical) & Done \\
IISS-753  & VPL IPV UI selector failure & Done \\
IISS-876  & VPL Pricing GTL incorrect booking & Done \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Note on IISS-487 and the CloudWatch Platform JIRAs.}
The Grafana AMG dashboard (IISS-487) is the primary deliverable and
subsumes much of the monitoring coverage originally scoped under
IISS-482--486. However, the CloudWatch platform work was not made redundant:
it established the architectural foundation (tag strategy, platform
inventory, Container Insights cost analysis, EKS scope) that IISS-487
built upon, and it produced the \texttt{ia-resources-monitoring} repository
with its own production-ready CloudWatch dashboard suite and registry API.
Both platforms coexist and serve different audiences --- the native
CloudWatch dashboards for per-service deep-dives, and Grafana AMG for
cross-platform operational oversight.

% ────────────────────────────────────────────────────────────
\section{Domain Immersion: Training Week (Apr 13--18, 2026)}
\label{sec:training_week}
% ────────────────────────────────────────────────────────────

Before meaningful SRE or automation work could begin, I required
deep domain context across all six IA platform environments. The week of
\textbf{Apr~13--18, 2026} was structured as a full-week knowledge
transfer (KT) programme led jointly by Horace and Kishor, and supplemented
by a multi-day CitcoWorks/Meridian architecture deep-dive delivered
remotely by Bhanu Teja Murari from Hyderabad. The training week
immediately followed the Apr~17 Meridian incident, giving the
sessions urgent practical relevance.

\subsection{RPA Support Processes}

The first two days (Apr~13--14) focused on the \textbf{Blue Prism and
UiPath} operational model. Key concepts covered:

\begin{itemize}
    \item \textbf{Blue Prism architecture} --- the pool/queue model (resource pools,
          foreground vs.\ background execution, licensed-per-node
          multithreading), the PROD and UAT folder hierarchy
          (\texttt{COE $\to$ DataOps $\to$ Processes / Assets / Rules /
          Queues}), and the ongoing migration path from Blue Prism to
          UiPath Cloud.

    \item \textbf{UiPath architecture} --- the Orchestrator folder structure
          (CoE, FA~DataOps, etc.), trigger types (scheduled, API, SQS),
          runtime node heartbeat model, and the runtime $\to$ node
          $\to$ Orchestrator status-update loop.

    \item \textbf{Support escalation model} --- service desk tickets escalate
          to Horace, then to Kishor for management-level decisions. RPA
          Support rotations follow a weekly cadence with a defined on-call
          protocol for time-sensitive fund operations.

    \item \textbf{VPL queue monitoring} --- starting at 5~PM EST, checking
          queue depth, bot count, and completion status. Delays in VPL
          directly affect NAV delivery to clients and are therefore treated
          as high-severity incidents.

    \item \textbf{Corporate Actions template management} --- Mandatory,
          Voluntary, and Ops Transfer workflows are handled through a
          combination of Blue Prism processes and manual exception review.
\end{itemize}

\subsection{CitcoWorks Platform}

The Apr~14--15 sessions (delivered by Bhanu Teja Murari) covered the
\textbf{CitcoWorks} platform in detail:

\begin{itemize}
    \item \textbf{Knowledge Graph (KG) hierarchy} --- a five-level hierarchy
          (L1: Product, L2: Process, L3: Sub-process, L4: Process Task,
          L5: Sub-task) maintained in a Neptune DB-backed graph and
          synchronized on-demand from the Optics source of truth. Each
          Friday, the team receives a workflow load email and updates the
          KG.

    \item \textbf{Event Routing Service and Action Processor} --- the ERS
          routes events (stored in ODL/Redis Elasticache, then Kafka) to
          registered APs based on the mapping table (Area, Subject, Source,
          Type). APs are deployed via \texttt{deployment\_specs.yaml} and
          implement business logic such as task status updates and
          cross-system integrations. The routing service cannot perform a
          pass-through; missing fields in advanced matching cause routing
          failures rather than silently dropped events.

    \item \textbf{AuthMaster} --- the Authentication and Authorization
          service governing user access to CitcoWorks. User personas are
          synced from AuthMaster into CitcoWorks at login, with a
          configurable TTL cache. Tenant roles determine which features and
          workflow views are accessible.
\end{itemize}

\subsection{Meridian and Event Store}

Building directly on the CitcoWorks session, the Apr~15--16 sessions
extended the architecture discussion to \textbf{Meridian event services}:

\begin{itemize}
    \item \textbf{Meridian event architecture} --- the interaction service,
          notification service, and task management API in the context of
          the Apr~17 incident. The session clarified how AXI polls
          Meridian for completion events and why OpenSearch consumer
          saturation blocked those queries.

    \item \textbf{Event Store} --- Citco's structured logging library used
          across all IA processes. Each event carries a mandatory set of
          fields: \texttt{tenant\_id}, \texttt{knowledge\_datetime},
          \texttt{area}, \texttt{subject}, \texttt{type}, \texttt{source},
          \texttt{code}, \texttt{message}, \texttt{run\_id},
          \texttt{process\_identifier}, and mutable context fields
          (client, fund, valueDate). Event codes follow a structured
          convention (\texttt{PROJ-I-0001} for informational,
          \texttt{PROJ-BE-0001} for business errors,
          \texttt{PROJ-SE-0001} for system errors) that enables
          query-based triage via CloudWatch Logs Insights.
\end{itemize}

\subsection{The \AE xeo\textsuperscript{\textregistered} Treasury Platform}

The Apr~16--17 sessions covered the
\textbf{\AE xeo\textsuperscript{\textregistered} Treasury platform} ---
Citco's cloud-based wire approval and fund movement visibility system.
\AE xeo integrates with the IA platform via COMAPI (a COM-based API
technology used for report generation) and the Aexeo Reporter Server REST
API (a Python-wrapped HTTP endpoint exposing the same report generation
capability without requiring a local COMAPI installation). Key workloads
supported by the IA platform include Active P\&L reports, Aexeo Report
Generation, and account manager configurations used by MESO and the
E-Binder reconciliation processes.

\subsection{Dashboard Feedback and IISS-461}

On \textbf{Apr~13} --- the first day of training week --- Kishor reviewed
an early prototype of the Lambda monitoring dashboard. His feedback
established the feature requirements that drove the remainder of the
IISS-487 dashboard build:

\begin{itemize}
    \item Add three additional tag dimensions to the dashboard filters:
          \textbf{BPM}, \textbf{CitcoWorks}, and \textbf{Meridian} (in
          addition to the existing IA-BPO and RPACFS1 tags).
    \item Improve filter logic to support multi-tag drill-down.
    \item Create a Confluence inventory page (tracked as \textbf{IISS-461})
          listing all Lambda functions by platform, tags, and purpose ---
          providing the human-readable complement to the automated inventory
          API.
\end{itemize}

This feedback loop --- domain immersion immediately producing actionable
engineering requirements --- was the pattern that characterized my entire
internship. The training week did not just provide background context; it
directly shaped my priority order, scope, and design of every subsequent
deliverable.

